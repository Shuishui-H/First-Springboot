# NOVA ERP V2 产品需求文档

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V2.0（评审稿） |
| 文档状态 | 待评审，确认后作为 V2 开发基线 |
| 版本定位 | 从“可演示的 V1”升级为“可持久化、可登录、可追溯的单机 ERP” |
| 适用系统 | NOVA ERP 进销存管理系统 |
| 技术约束 | Vue 3 + Spring Boot 3.4 + JDK 17 + MySQL 8 |

## 1. V2 背景

V1 已完成商品、采购、销售、仓储、业务报表和系统设置的页面及核心业务闭环。采购入库、销售出库、销售退货可带动商品库存和仓储流水变化，能够用于课程演示。

但 V1 的核心业务数据仍主要保存在 Spring Boot 内存中，服务重启后会恢复为演示数据；系统设置中的用户、角色和基础资料则保存于浏览器 localStorage。不同模块并未完全共用同一份主数据，真实登录、后端权限控制、操作审计和数据库事务也尚未建立。

V2 的任务不是增加财务、供应链等大模块，而是先把 V1 的业务闭环变成可长期保存、可被多人按权限使用、可追溯的系统。

## 2. 产品目标

### 2.1 总目标

将 ERP 的商品、基础资料、业务单据、库存余额、库存流水、用户和权限统一保存到 MySQL；所有业务操作通过后端接口和数据库事务完成，确保库存、单据和报表数据一致。

### 2.2 V2 成功标准

1. 服务重启后，业务数据、设置数据和用户权限不丢失。
2. 商品、仓库、供应商和客户只维护一份主数据，采购、销售、仓储页面均引用同一数据源。
3. 用户可登录；菜单可见范围和后端接口访问范围均受角色权限控制。
4. 采购入库、销售出库、销售退货在数据库事务内完成，不能出现“单据已确认但库存未变化”或重复扣减库存。
5. 报表按数据库中的已确认业务数据实时统计，支持日期筛选和 CSV 导出。
6. 关键操作可追溯到操作用户、时间、来源单据和前后状态。

## 3. 用户角色与权限范围

| 角色 | V2 可操作范围 | V2 不可操作范围 |
| --- | --- | --- |
| 系统管理员 | 用户、角色、权限、基础资料、全量查询 | 不能绕过库存单据直接修改库存 |
| 采购员 | 新建、编辑、提交采购订单；查看供应商与采购数据 | 审核采购单、确认入库、管理用户 |
| 采购主管 | 审核、驳回采购订单；查看采购报表 | 确认入库、管理用户 |
| 销售员 | 新建、编辑、提交销售订单；查看客户与销售数据 | 审核销售单、确认出库、管理用户 |
| 销售主管 | 审核、驳回销售订单；查看销售报表 | 确认出库、管理用户 |
| 仓库管理员 | 确认采购入库、销售出库、销售退货；查看库存与流水 | 审核采购/销售单、修改售价、管理用户 |
| 经营管理者 | 查看全部报表和单据 | 新增、审核、出入库、系统设置 |

> 前端隐藏按钮仅用于改善体验；后端必须依据登录用户的权限再次校验。

## 4. V2 范围与优先级

### 4.1 Must：本期必须交付

| 编号 | 模块 | 功能 | 说明 |
| --- | --- | --- | --- |
| INF-01 | 数据库 | MySQL 初始化与建表 | 建立可重复执行的建表/初始化方案，开发、测试环境均可独立创建数据库 |
| INF-02 | 数据库 | 数据访问层改造 | 将 V1 内存数据改为 MySQL 持久化，不再以重启恢复演示数据作为正常运行方式 |
| MDM-01 | 主数据 | 商品持久化 | 商品编码唯一；商品库存字段改为汇总展示，不允许通过商品编辑直接修改真实库存 |
| MDM-02 | 主数据 | 仓库、供应商、客户统一 | 系统设置、采购、销售、仓储使用同一套后端主数据接口 |
| AUTH-01 | 身份认证 | 登录、退出、当前用户 | 支持账号密码登录、登录态校验、退出；密码只保存哈希值 |
| AUTH-02 | 权限 | 角色与接口鉴权 | 用户绑定角色，角色绑定权限；无权访问接口时返回 403 |
| PUR-21 | 采购 | 采购订单与入库落库 | 订单、明细、审核记录、入库单和明细均持久化 |
| SAL-21 | 销售 | 销售订单、出库、退货落库 | 订单、明细、出库单、退货单和明细均持久化 |
| INV-21 | 库存 | 库存余额与库存流水事务化 | 入库、出库、退货操作需在同一数据库事务中更新库存、单据状态和流水 |
| RPT-21 | 报表 | 数据库实时统计 | 看板、采购分析、销售分析、库存余额、库存预警读取已确认业务数据 |
| SYS-21 | 系统设置 | 设置数据落库 | 用户、角色、权限、仓库、供应商、客户不再仅保存在浏览器 localStorage |
| OPS-01 | 审计 | 关键操作日志 | 记录登录、单据状态变化、出入库、主数据启停、权限变更 |

### 4.2 Should：资源允许时交付

| 编号 | 模块 | 功能 | 说明 |
| --- | --- | --- | --- |
| INV-22 | 库存 | 并发库存校验 | 销售出库采用乐观锁或等价机制，避免两个操作同时超卖 |
| INV-23 | 库存 | 库存调整单 | 允许管理员/仓管通过“盘点调整单”修正库存，必须填写原因并生成流水 |
| RPT-22 | 报表 | 日期与维度筛选 | 支持日期范围、商品、仓库、供应商、客户等筛选；默认当前自然月 |
| RPT-23 | 报表 | CSV 导出与页面一致 | 导出的列、筛选条件和记录数与当前页面一致 |
| OPS-02 | 体验 | 分页、排序与统一错误提示 | 列表类接口支持后端分页；重复提交、权限不足、库存不足提示清晰 |
| DATA-01 | 数据 | V1 演示数据导入 | 提供可选初始化脚本，导入 V1 商品和基础资料作为演示数据 |

### 4.3 Won't：明确不在 V2 交付

- 财务核算、凭证、发票、应收应付、成本结转。
- 多公司、多组织、多币种、多语言。
- 批次、序列号、保质期、货位和 PDA 扫码。
- 多级审批、工作流引擎、电子签章、消息中心。
- 公网部署、SSO、短信/邮件通知、第三方物流或电商平台集成。

## 5. 核心数据与一致性规则

### 5.1 数据实体

| 实体 | 关键字段 | 关系与规则 |
| --- | --- | --- |
| 用户 | 用户名、密码哈希、姓名、状态、角色 | 用户名唯一；停用用户不可登录 |
| 角色/权限 | 角色编码、名称、状态、权限编码 | 角色停用后，该角色用户立即失去业务访问权限 |
| 商品 | 编码、名称、分类、单位、售价、安全库存、状态 | 商品编码唯一；库存由库存余额汇总展示 |
| 仓库 | 编码、名称、负责人、地址、状态 | 仓库编码唯一；停用仓库不可用于新单据 |
| 供应商/客户 | 编码、名称、联系人、电话、状态 | 编码唯一；停用后仅保留历史单据查看 |
| 采购订单/明细 | 单号、供应商、状态、商品、数量、单价 | 已审核订单可分批入库 |
| 销售订单/明细 | 单号、客户、仓库、状态、商品、数量、成交价 | 已审核订单可分批出库 |
| 入库/出库/退货单 | 单号、来源单据、仓库、状态、明细 | 已确认后不可编辑或重复确认 |
| 库存余额 | 商品、仓库、现存量、可用量、版本号 | 商品+仓库唯一；用于出库校验和并发控制 |
| 库存流水 | 业务类型、变动数量、来源单号、操作人、时间 | 仅追加，不允许修改或删除 |
| 操作日志 | 模块、动作、业务编号、操作人、时间、摘要 | 用于业务追溯，不存储密码等敏感数据 |

### 5.2 库存口径

| 场景 | 库存余额变化 | 库存流水类型 |
| --- | --- | --- |
| 采购入库确认 | 增加实收数量 | 采购入库 |
| 销售出库确认 | 减少发货数量 | 销售出库 |
| 销售退货确认 | 增加退回数量 | 销售退货 |
| 盘盈/盘亏确认（Should） | 按调整差额增减 | 库存调整 |

通用规则：

1. 所有库存变化必须由已确认的业务单据触发，不提供“直接修改库存”接口。
2. 确认操作必须幂等：同一张单据重复提交或重复请求，库存只能变化一次。
3. 销售出库时必须校验商品、仓库和可用库存；库存不足时，整个确认操作失败且数据库不产生部分更新。
4. 采购入库、销售出库、销售退货的“单据状态更新、库存余额更新、库存流水新增、操作日志新增”必须在同一数据库事务内完成。
5. 商品、仓库、供应商、客户停用后，历史数据保持可查，不能被删除造成历史单据断链。

## 6. 功能需求

### 6.1 数据库与数据迁移

| 编号 | 需求 | 验收标准 |
| --- | --- | --- |
| INF-01 | 项目提供数据库配置模板，不提交真实密码；支持通过环境变量或本地配置文件指定数据库连接 | 新成员按说明创建数据库并启动后端后，可正常访问系统 |
| INF-02 | 提供版本化建表/初始化脚本，重复执行不破坏已有业务数据 | 空数据库可建立完整表结构；已有数据库升级不会丢失有效数据 |
| DATA-01 | 可选导入商品、仓库、供应商、客户和演示账号 | 导入后可完成一条采购入库和销售出库演示流程 |

### 6.2 登录与权限

| 编号 | 需求 | 规则 | 验收标准 |
| --- | --- | --- | --- |
| AUTH-01 | 登录 | 用户名、密码均正确且用户、角色均启用才可登录 | 正确账号可进入系统；错误密码、停用用户、停用角色均不能登录 |
| AUTH-02 | 当前用户与退出 | 页面展示当前用户、角色；退出后清除登录态 | 退出后访问受保护页面会回到登录页 |
| AUTH-03 | 菜单权限 | 根据权限控制系统设置和业务菜单入口 | 销售员不可看到用户管理；经营管理者仅可查看报表 |
| AUTH-04 | 接口权限 | 后端对写操作和敏感查询校验权限 | 手动调用无权限接口返回 403，且不改变数据 |
| AUTH-05 | 密码安全 | 仅保存密码哈希；重置密码需记录日志 | 任一用户查询接口均不返回密码或哈希字段 |

### 6.3 统一基础资料

| 编号 | 需求 | 规则 | 验收标准 |
| --- | --- | --- | --- |
| MDM-01 | 商品管理 | 商品编码唯一；安全库存可编辑，真实库存只读 | 修改商品不会直接改变库存余额或库存流水 |
| MDM-02 | 仓库管理 | 仓库编码唯一；停用仓库不可用于新入出库单 | 停用后新单据下拉中不可选，历史单据仍可显示名称 |
| MDM-03 | 供应商管理 | 采购订单仅可选择启用供应商 | 新建采购单下拉与系统设置供应商数据一致 |
| MDM-04 | 客户管理 | 销售订单仅可选择启用客户 | 新建销售单下拉与系统设置客户数据一致 |

### 6.4 采购、销售与库存改造

| 编号 | 需求 | 规则 | 验收标准 |
| --- | --- | --- | --- |
| PUR-21 | 采购订单落库 | 新建、提交、审核、驳回、查看明细均保存至数据库 | 重启服务后采购订单状态和明细保持不变 |
| PUR-22 | 采购入库落库 | 支持按采购订单分批入库；实收数量不得超过剩余数量 | 两次分批入库后，订单、库存余额、流水数量完全一致 |
| SAL-21 | 销售订单落库 | 新建、提交、审核、驳回、查看明细均保存至数据库 | 重启服务后销售订单状态和明细保持不变 |
| SAL-22 | 销售出库落库 | 确认时校验库存；支持分批出库 | 库存不足时出库失败且无流水；成功时数量与流水一致 |
| SAL-23 | 销售退货落库 | 必须引用已确认出库单；退货量不超过可退数量 | 确认退货后库存回增，且原出库单可退数量正确减少 |
| INV-21 | 库存余额与流水 | 按商品+仓库保存余额；所有变化写流水 | 从任意余额记录可查询到关联业务流水 |

### 6.5 报表与导出

| 编号 | 需求 | 规则 | 验收标准 |
| --- | --- | --- | --- |
| RPT-21 | 经营看板 | 展示采购金额、销售金额、入库量、出库量、库存货值、预警数量和趋势 | 新确认一笔出入库后，刷新看板数据与单据一致 |
| RPT-22 | 分析报表 | 采购、销售、库存余额、库存预警支持日期范围和业务维度筛选 | 修改筛选条件后，页面与导出结果均按条件变化 |
| RPT-23 | CSV 导出 | 仅导出当前用户有权查看且符合当前筛选条件的数据 | 导出列、记录数与页面列表一致；文件可被 Excel 正常打开 |

### 6.6 操作日志

| 编号 | 需求 | 记录内容 | 验收标准 |
| --- | --- | --- | --- |
| OPS-01 | 登录日志 | 用户、结果、时间、IP（可获取时） | 能查询成功和失败登录记录，不暴露密码 |
| OPS-02 | 业务日志 | 单据创建、提交、审核、驳回、确认、退货、作废 | 查看单据时可定位最近关键操作人和时间 |
| OPS-03 | 设置日志 | 用户启停、角色权限变更、主数据启停 | 修改后保留操作前后摘要和操作人 |

## 7. 页面与交互原则

1. 保留 V1 已形成的左侧业务导航和模块入口，数据库改造不要求推翻现有视觉风格。
2. 所有写操作提供成功或失败提示；库存不足、重复确认、无权限、数据已变化等场景需使用业务语言说明原因。
3. 单据详情展示单号、状态、创建人、审核人、确认人、时间和关联来源；库存流水可回跳到来源单据。
4. 列表默认分页，支持常用条件筛选和空状态；不在前端一次性加载全部历史数据。
5. 系统设置页面不再以 localStorage 作为业务事实来源；localStorage 仅可保存登录态、界面偏好等非关键临时信息。

## 8. 非功能需求

| 分类 | V2 要求 |
| --- | --- |
| 数据安全 | 数据库账号、密码、密钥不提交到 Git；提供 `.example` 配置文件 |
| 一致性 | 关键库存操作必须使用数据库事务；失败时整体回滚 |
| 性能 | 普通列表查询默认 20 条/页；常用查询条件建立索引 |
| 可维护性 | 控制器、服务、数据访问、实体/DTO 分层；统一异常返回结构 |
| 可观测性 | 后端日志包含请求失败和关键业务异常，不记录明文密码 |
| 备份 | 提供开发环境导出/导入数据库的操作说明 |

## 9. V2 验收场景

### 场景 A：持久化与统一主数据

1. 管理员在系统设置新增供应商、客户和仓库。
2. 刷新页面、重启前后端服务。
3. 在采购、销售、仓储页面分别查询和选择这些资料。

**通过标准：**资料未丢失，且三个业务模块读取的是同一份数据。

### 场景 B：采购入库

1. 采购员新建并提交采购订单，采购主管审核通过。
2. 仓管员分两次确认入库。
3. 查询采购订单、库存余额、库存流水和采购报表。

**通过标准：**订单累计实收、库存增长、两笔流水和报表金额一致；重启服务后数据仍存在。

### 场景 C：销售出库与退货

1. 销售员新建并提交销售订单，销售主管审核通过。
2. 仓管员确认销售出库，再登记部分销售退货。
3. 查询订单履约数量、库存余额、库存流水和销售报表。

**通过标准：**出库减少、退货回增，退货量不超过原出库量，所有数据可追溯到来源单据。

### 场景 D：权限控制

1. 使用销售员账号登录，尝试进入用户管理或直接调用用户管理接口。
2. 使用管理员账号登录，调整销售员角色权限后再次登录。

**通过标准：**销售员无权时菜单不可见、接口返回 403；授权后按新权限生效，且变更有操作日志。

## 10. 版本边界与后续方向

V2 完成的标志是：V1 的核心流程由“页面演示 + 内存数据”升级为“数据库持久化 + 登录权限 + 事务库存 + 可追溯报表”。

在 V2 稳定后，再评估 V3：库存调拨/盘点、采购退货、销售价格策略、审批流、批次库位、财务对接和部署监控。

