# ERP 项目开发环境安装与配置指南（Windows 10/11）

本文用于手动搭建本课程 ERP 项目所需的开发环境，并让它们能在 VS Code 的集成终端中直接使用。

本文采用的技术组合是：Spring Boot（后端）+ Vue 3（前端）+ MySQL（数据库）+ Git（版本管理）。以下版本已经在本机验证可用：JDK 21、Maven 3.9.16、Node.js 24、MySQL 8.3、Git 2.52。

> 不需要为了和本文完全一致而重装已有的 JDK、Node.js、Git 或 MySQL。先使用已有版本即可；只有项目实际报兼容性错误时，才考虑替换版本。

## 1. 安装清单与 PATH 要求

| 工具 | 本项目建议/已用版本 | 用途 | 是否需要加入 PATH |
| --- | --- | --- | --- |
| VS Code | 最新稳定版 | 编辑、调试、集成终端 | 安装时勾选即可，非必须 |
| JDK | 21 LTS | 编译、运行 Spring Boot | **需要**：`JDK\bin` |
| Maven | 3.9.16 | 下载 Java 依赖、构建后端 | **需要**：`Maven\bin`；另设 `MAVEN_HOME` |
| Node.js | 24 LTS | 运行 Vue、npm 包管理 | 安装程序通常自动加入 |
| Git | 2.x | 本地提交、推送远程仓库 | 安装程序选择加入 PATH |
| MySQL Server | 8.3 | 关系型数据库 | 推荐加入：`MySQL Server 8.3\bin` |

“加入 PATH”表示：在任意新开的 PowerShell、命令提示符或 VS Code 终端中，可以直接输入命令。例如输入 `java -version` 或 `mysql --version`，不用写完整安装路径。

## 2. 通用：如何修改 Windows 用户 PATH

除非特别说明，优先改“用户变量”，不必改“系统变量”。

1. 按 Windows 键，搜索并打开 **编辑账户的环境变量**。
2. 点击右下角 **环境变量**。
3. 在上半部分“用户变量”中选中 `Path`，点击 **编辑**。
4. 点击 **新建**，每个目录单独添加一行；不要删除原有条目。
5. 一路点击“确定”保存。
6. **完全关闭并重新打开** PowerShell、VS Code；已经打开的终端不会自动读取新的 PATH。

验证某个命令是否已被识别：

```powershell
Get-Command java
Get-Command mvn
Get-Command node
Get-Command git
Get-Command mysql
```

## 3. 安装 VS Code

官方入口：[VS Code Windows 下载页](https://code.visualstudio.com/download)。

1. 选择 **Windows → User Installer → x64**。普通 Intel/AMD 的 Windows 电脑通常选 x64；只有 ARM Windows 设备才选 Arm64。
2. 双击下载的安装程序，按默认路径安装即可。
3. 安装向导若出现以下选项，建议勾选：
   - `Add to PATH`（把 `code` 命令加入 PATH）
   - `Add "Open with Code" action to Windows Explorer`（可选，右键用 VS Code 打开文件夹）
4. 打开 VS Code，按 `Ctrl+Shift+P`，输入并选择 `Shell Command: Install 'code' command in PATH`。如果该命令不可用，不影响正常使用。

验证：重新打开 PowerShell，执行：

```powershell
code --version
```

## 4. 安装 JDK 21（Java）

### 4.1 版本选择

推荐 **JDK 21 LTS**。它能运行本项目的 Spring Boot 版本，也比短期版本稳定。JDK 必须安装“JDK”，不要只安装 JRE。

官方下载：[Eclipse Temurin JDK 发布页](https://adoptium.net/temurin/releases/)。页面中选择：

- Version：`JDK 21 - LTS`
- Operating System：Windows
- Architecture：x64
- Package Type：JDK
- 文件：`.msi`

也可以使用已安装的 Microsoft OpenJDK、Oracle JDK 等发行版；同一台电脑不建议长期混用多个未明确配置的 JDK。

### 4.2 安装与环境变量

安装时如果提供“设置 `JAVA_HOME`”或“添加到 PATH”的选项，可以勾选。若没有自动设置，则手动配置：

1. 在“用户变量”点击 **新建**：

   ```text
   变量名：JAVA_HOME
   变量值：你的 JDK 根目录，例如 F:\Java_JDK\JDK21
   ```

2. 编辑用户 `Path`，新增：

   ```text
   %JAVA_HOME%\bin
   ```

`JAVA_HOME` 指向 JDK 根目录，不要指向 `bin` 目录。

### 4.3 验证

```powershell
java -version
javac -version
echo $env:JAVA_HOME
```

应能看到 Java 21 和 `javac` 版本。若 `java` 有版本但 `javac` 不存在，通常是误装了 JRE，应改装 JDK。

## 5. 安装 Maven 3.9.16

Maven 用于下载 Spring Boot、MyBatis 等 Java 依赖，并负责打包、测试、运行后端。

官方下载：[Apache Maven 下载页](https://maven.apache.org/download.cgi)。选择 **Binary zip archive**，不要下载 source zip，也不要下载 Maven 4 的预览版。

本项目使用：`apache-maven-3.9.16-bin.zip`。Apache 官方将 3.9.16 标为当前推荐的 Maven 3 稳定版本。

### 5.1 解压

1. 解压 zip；解压后的目录应该包含 `bin`、`conf`、`lib` 等子目录。
2. 建议放在一个不含中文、空格较少的稳定目录，例如：

   ```text
   C:\Users\你的用户名\AppData\Local\Programs\Apache\apache-maven-3.9.16
   ```

   本机实际目录为：

   ```text
   C:\Users\shuishui\AppData\Local\Programs\Apache\apache-maven-3.9.16
   ```

3. 不要把 `bin` 文件夹单独搬走；Maven 需要保持完整目录结构。

可选安全检查：下载页同时提供 `.sha512` 校验文件，可用 PowerShell 核对下载文件：

```powershell
Get-FileHash .\apache-maven-3.9.16-bin.zip -Algorithm SHA512
```

### 5.2 配置环境变量

1. 新建用户变量：

   ```text
   变量名：MAVEN_HOME
   变量值：C:\Users\shuishui\AppData\Local\Programs\Apache\apache-maven-3.9.16
   ```

2. 编辑用户 `Path`，新增：

   ```text
   %MAVEN_HOME%\bin
   ```

不需要设置旧教程中常见的 `M2_HOME`；使用 `MAVEN_HOME` 即可。

### 5.3 验证

重新打开终端：

```powershell
mvn -v
```

应显示 Maven 3.9.16、Maven home 和 Java 21。若提示“`mvn` 无法识别”，通常是 PATH 没加对或终端没有重启。

## 6. 安装 Node.js 与 npm

Node.js 用来运行 Vue 开发服务器；npm 会随 Node.js 一起安装，用于安装前端依赖。

官方下载：[Node.js 下载页](https://nodejs.org/en/download)。优先选择 **LTS** 的 Windows `.msi` 安装包。本文环境使用 Node.js 24；Node.js 官方当前将 v24 列为 LTS 分支。

安装过程：

1. 双击 `.msi`，按默认项完成。
2. 保持 npm 组件被勾选。
3. 保持“添加到 PATH”相关选项被勾选。
4. 不需要额外安装 npm。

验证：

```powershell
node -v
npm -v
```

如遇网络慢、安装依赖失败，后续可以单独配置 npm 镜像；当前不要为了“预防”而修改镜像地址，以免来源和排错变复杂。

## 7. 安装 MySQL 8.3

MySQL 是后端的数据存储。这里需要安装的是 **MySQL Server**，不是只有 Workbench 客户端。

官方下载：[MySQL Installer for Windows](https://dev.mysql.com/downloads/installer/)。下载页面可能要求登录，选择页面下方的 **No thanks, just start my download** 即可直接下载。

### 7.1 安装选择

1. 下载并运行 MySQL Installer。
2. 安装类型可以选 `Developer Default`（含 Server、Workbench 等常用工具），或选择 `Custom` 后至少勾选：
   - MySQL Server 8.3
   - MySQL Workbench（推荐，用图形界面管理数据库）
   - MySQL Shell（可选）
3. 配置类型选择 `Development Computer`。
4. 网络端口保持默认：`3306`。
5. 认证方式选默认的强密码认证即可。
6. 设置 root 密码，并妥善保存；后续连接数据库必须使用它。
7. Windows Service 名称可使用默认值，例如 `MySQL83`；勾选开机自动启动。
8. 点击 Apply 执行配置，直至所有步骤显示完成。

### 7.2 配置 MySQL 的 PATH

若安装目录是默认目录，在用户 `Path` 新增：

```text
C:\Program Files\MySQL\MySQL Server 8.3\bin
```

这是 `mysql.exe` 所在的文件夹，而不是 `C:\Program Files\MySQL`。

### 7.3 验证、登录和退出

重新打开终端后执行：

```powershell
mysql --version
mysql -u root -p
```

第二条命令会提示输入密码。输入时屏幕不会显示字符，这是正常的。成功后会进入 `mysql>` 提示符。

可执行以下 SQL 检查：

```sql
SELECT VERSION();
SHOW DATABASES;
```

退出 MySQL：

```sql
exit;
```

若终端能执行 `mysql --version`，但连接时提示“Access denied”，说明 PATH 和服务没问题，只是用户名或密码不正确。

若提示无法连接到 `localhost:3306`，先按 Windows 键搜索“服务”，确认 `MySQL83` 状态为“正在运行”。

## 8. 安装 Git 并做首次配置

官方下载：[Git for Windows](https://git-scm.com/download/win)。运行安装程序时，大部分选项保留默认即可。

在选择 PATH 的页面，选择包含以下含义的选项：

```text
Git from the command line and also from 3rd-party software
```

它会让 Git 同时能被 PowerShell、VS Code 和其他工具调用。

安装后验证：

```powershell
git --version
```

首次提交前要设置署名（只需一次）：

```powershell
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

检查配置：

```powershell
git config --global --list
```

Git 本地提交不需要登录。只有第一次向 GitHub、Gitee 等远程平台执行 `git push` 时，才需要在浏览器登录或配置个人访问令牌/SSH 密钥。

## 9. VS Code 必装扩展与集成方式

在 VS Code 左侧点击“扩展”（快捷键 `Ctrl+Shift+X`），搜索并安装以下扩展。也可按 `Ctrl+Shift+P`，使用 `Extensions: Install Extensions`。

| 扩展名称 | 扩展 ID | 用途 |
| --- | --- | --- |
| Extension Pack for Java | `vscjava.vscode-java-pack` | Java 语言服务、调试、测试等基础能力 |
| Maven for Java | `vscjava.vscode-maven` | 识别 `pom.xml`、运行 Maven 任务 |
| Spring Boot Extension Pack | `vmware.vscode-boot-dev-pack` | Spring Boot 运行、面板、初始化支持 |
| Vue - Official | `vue.volar` | Vue 3 语法、跳转、类型提示 |
| ESLint | `dbaeumer.vscode-eslint` | 前端代码规范检查 |
| Prettier - Code formatter | `esbenp.prettier-vscode` | 代码格式化 |
| SQLTools | `mtxr.sqltools` | 在 VS Code 中连接数据库 |
| SQLTools MySQL/MariaDB Driver | `mtxr.sqltools-driver-mysql` | SQLTools 的 MySQL 驱动 |
| Lombok Annotations Support | `vscjava.vscode-lombok` | 正确识别后端的 Lombok 注解 |
| REST Client | `humao.rest-client` | 在 `.http` 文件中测试接口（可选） |

Java 扩展官方页面：[Extension Pack for Java](https://marketplace.visualstudio.com/items?itemName=vscjava.vscode-java-pack)。Vue 扩展官方页面：[Vue - Official](https://marketplace.visualstudio.com/items?itemName=Vue.volar)。

### 9.1 在 VS Code 中打开终端

打开项目文件夹后，按 ``Ctrl+` ``（反引号）打开集成终端。它与 PowerShell 终端一样会读取 PATH，所以可以运行：

```powershell
java -version
mvn -v
node -v
npm -v
git --version
mysql --version
```

### 9.2 指定 VS Code 使用的 Maven

若电脑上安装过多个 Maven，建议在项目根目录创建 `.vscode/settings.json`，指定固定版本。本项目已经配置为：

```json
{
  "maven.executable.path": "C:\\Users\\shuishui\\AppData\\Local\\Programs\\Apache\\apache-maven-3.9.16\\bin\\mvn.cmd"
}
```

该配置只影响这个项目，可以避免 VS Code 误用其他 Maven。实际文件见：[.vscode/settings.json](.vscode/settings.json)。

### 9.3 连接 MySQL（后续创建数据库时使用）

安装 SQLTools 和 MySQL Driver 后：

1. 按 `Ctrl+Shift+P`，选择 `SQLTools: Add New Connection`。
2. 选择 MySQL/MariaDB。
3. 按实际安装填写：
   - Host：`localhost`
   - Port：`3306`
   - User：`root`（后续推荐新建项目专用用户）
   - Password：安装 MySQL 时设置的密码
   - Database：项目创建后再填写
4. 点击 Test Connection，成功后保存。

不要把 root 密码写进 Git 仓库；后端的数据库密码后续应放在本机不提交的配置中，或使用环境变量。

## 10. 本机当前安装状态记录

| 项目 | 当前状态/位置 |
| --- | --- |
| JDK | OpenJDK 21.0.8，`F:\Java_JDK\JDK21` |
| Maven | 3.9.16，`C:\Users\shuishui\AppData\Local\Programs\Apache\apache-maven-3.9.16` |
| Node.js | v24.14.0 |
| Git | 2.52.0.windows.1 |
| MySQL | 8.3.0，服务名 `MySQL83`，端口 3306 |
| VS Code | 已安装 Java、Maven、Spring Boot、Vue、SQLTools 等扩展 |

## 11. 总体验收清单

新开一个 PowerShell 或 VS Code 集成终端，逐条执行：

```powershell
java -version
javac -version
mvn -v
node -v
npm -v
git --version
mysql --version
```

全部能返回版本号，就说明基础环境已完成。

还可以检查 MySQL 是否能登录：

```powershell
mysql -u root -p
```

## 12. 常见问题速查

### 12.1 “无法将 xxx 识别为 cmdlet”

表示该程序所在目录没有正确加入 PATH，或 PATH 修改后终端没有重启。检查是否把“真正包含 `xxx.exe` 或 `xxx.cmd` 的 `bin` 目录”加入了 PATH。

### 12.2 Maven 显示的 Java 版本不对

先执行：

```powershell
where.exe java
java -version
echo $env:JAVA_HOME
```

确认 `JAVA_HOME` 和 PATH 中的 JDK `bin` 指向同一版本。重启 VS Code 后，再检查 Maven。

### 12.3 VS Code 找不到 Java 或 Maven

1. 在 VS Code 中执行 `Developer: Reload Window`。
2. 在新的集成终端运行 `java -version`、`mvn -v`。
3. 检查 Java 扩展和 Maven for Java 是否已启用。
4. 检查 `.vscode/settings.json` 中的 Maven 路径是否真实存在。

### 12.4 MySQL 服务没有启动

打开 Windows“服务”，找到 `MySQL83` 并启动；也可用管理员 PowerShell：

```powershell
Get-Service MySQL83
Start-Service MySQL83
```

### 12.5 是否需要登录各种软件

- VS Code：本地开发不需要登录。
- Maven、JDK、Node.js：不需要登录。
- MySQL：连接数据库时要输入数据库账户密码，不是登录官网。
- Git：本地 `commit` 不需要登录；推送到 GitHub/Gitee 时需要登录或使用令牌/SSH。

## 13. 环境完成后的下一步

环境安装完成后，才开始创建项目：建立后端 Spring Boot 工程、建立 Vue 3 前端工程、创建数据库、配置数据库连接，并初始化 Git 仓库。当前阶段不需要提前创建这些内容。
