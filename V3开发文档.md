# 业务背景

NOVA ERP 当前已完成采购、销售、销售退货、仓库、报表、经营概览等 V1 功能，并通过 MySQL 的 `erp_application_state` 表保存整份运行状态，因此重启后业务数据不会恢复为初始演示值。

但该快照并不是正式的业务数据模型：采购订单、销售订单、库存余额、库存流水等虽然已有对应的 MySQL 表结构，却尚未成为运行时唯一读写来源；同时，系统没有登录页、后端鉴权、调拨、盘点以及完整的操作审计。V3 的目标是在不破坏已有业务数据的前提下，完成“关系化数据 + 多用户权限 + 库存闭环”。

本期仅实现登录页所必需的基础品牌视觉。采购、销售、仓库、报表等业务页面的整体布局、配色和响应式重构明确留给 V4。

# 功能梳理

| 编号 | 功能 | 前端实现 | 后端实现 | 优先级 |
| --- | --- | --- | --- |
| DAT-31 | 关系化数据切换 | 页面接口保持业务语义不变；加载时显示统一错误提示 | 将领域服务从状态快照切换到 MySQL 明细表、余额表、流水表读写 | Must |
| DAT-32 | 迁移与对账 | 管理员可查看迁移状态与失败提示，不提供普通用户触发入口 | 一次性迁移现有快照数据；记录迁移状态、数量与校验结果 | Must |
| AUTH-31 | 登录页与登录态 | 新增 `LoginView.vue`；未登录不可进入业务页；成功后进入经营概览 | Spring Security 会话认证、当前用户、退出、密码 BCrypt 校验 | Must |
| AUTH-32 | 角色与权限 | 按权限显示菜单和操作按钮；403 时提示“无权操作” | 角色权限从 `sys_role_permission` 读取；接口级二次鉴权 | Must |
| INV-31 | 仓库间调拨 | 仓库模块新增调拨单列表、创建、详情、确认、取消待确认单 | 调拨单/明细落库；确认时两个仓库余额与两条流水同步变化 | Must |
| INV-32 | 库存盘点 | 仓库模块新增盘点单列表、创建、实盘录入、确认、详情 | 盘点单/明细落库；差异确认后更新余额并生成盘盈/盘亏流水 | Must |
| INV-33 | 库存一致性 | 操作前展示二次确认；库存不足、数据已变化、重复提交给出业务提示 | `@Transactional`、乐观锁/条件更新、单据状态校验、请求幂等 | Must |
| OPS-31 | 审计与追溯 | 单据详情显示创建/确认记录；流水显示来源单据和操作人 | 复用 `sys_operation_log`，记录关键操作；库存流水关联来源单据 | Must |
| RPT-31 | 统一统计 | 经营概览、商品档案、报表读取同一口径；风险项可跳转创建调拨 | 全部统计改从关系表读取；主仓风险同时返回总库存与主仓可用库存 | Must |
| AUTH-33 | 账号安全 | 管理员重置密码、首次登录修改密码、登录失败提示 | 密码重置、失败次数/锁定策略、审计记录 | Should |
| INV-34 | 调拨在途 | 调拨单展示发运、在途、收货状态 | 将一次确认调拨扩展为发运/收货两步库存状态变更 | Should |
| INV-35 | 库存预留 | 已审核销售单显示已预留量和可用量 | 已审核销售单锁定库存，出库或取消时释放 | Should |

前端组件与后端模块建议如下：

```text
erp-web/src
├─ LoginView.vue
├─ App.vue（认证初始化、路由守卫、权限菜单）
├─ api/auth.js、api/transfer.js、api/stocktake.js
└─ WarehouseView.vue
   ├─ TransferList / TransferForm / TransferDetail
   └─ StocktakeList / StocktakeForm / StocktakeDetail

erp-server/src/main/java/com/erp/demo
├─ auth（SecurityConfig、AuthController、AuthService、CurrentUser）
├─ persistence（实体、Repository、快照迁移服务）
├─ inventory（TransferController/Service、StocktakeController/Service）
├─ audit（OperationLogService）
└─ report、overview（改为查询关系化数据）
```

# 数据库设计

## 数据切换原则

现有 `V1__create_core_schema.sql` 已创建 `sys_*`、`md_*`、`pur_*`、`sal_*`、`wh_inventory_balance`、`wh_stock_flow` 等关系表；V3 不重复建同名表。

新增 Flyway 迁移建议按顺序实施：

1. `V6__create_transfer_and_stocktake.sql`：创建调拨、盘点及迁移记录表。
2. `V7__extend_user_and_business_indexes.sql`：补充账号安全字段、状态/日期查询索引及必要的外键。
3. Java 迁移服务读取一次旧 `erp_application_state`，写入关系表并将结果写入 `sys_data_migration`。迁移成功后，领域服务只读写关系表；旧快照表保留为只读回退依据，不再随业务操作更新。

迁移前必须先执行 MySQL 备份；迁移任务不可由普通前端接口触发。若对账失败，事务回滚并保留旧快照，系统不得进入半迁移状态。

## 表名：现有核心表（V3 启用）

| 表名 | V3 用途 | 关键约束/说明 |
| --- | --- | --- |
| `sys_user` | 登录用户、状态、角色 | `username` 唯一；`password_hash` 仅存 BCrypt 哈希；关联 `sys_role` |
| `sys_role` / `sys_permission` / `sys_role_permission` | 角色与权限定义 | 权限编码唯一；角色与权限为多对多 |
| `sys_operation_log` | 登录、单据、库存、权限审计 | 仅追加；记录操作人、业务编号、前后摘要和时间 |
| `md_product` | 商品主数据、安全库存 | `sku` 唯一；不直接保存真实库存 |
| `md_warehouse` | 仓库主数据 | `code` 唯一；停用仓库不能用于新单据 |
| `md_supplier` / `md_customer` | 供应商、客户主数据 | 停用后保留历史关联 |
| `pur_purchase_order` 及明细、收货单及明细 | 采购业务 | 已确认入库驱动库存增加 |
| `sal_sales_order` 及明细、出库单及明细、退货单及明细 | 销售业务 | 已确认出库/退货驱动库存变化 |
| `wh_inventory_balance` | 商品+仓库库存余额 | `(warehouse_id, product_id)` 唯一；`version` 用于并发控制 |
| `wh_stock_flow` | 不可变库存流水 | 来源+商品+业务类型唯一，避免同一来源重复生成流水 |

## 表名：wh_transfer_order（新增）

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| id | BIGINT | PK，自增 | 调拨单主键 |  |
| transfer_no | VARCHAR(50) | NOT NULL，UNIQUE | 调拨单号 | 格式 `TRFyyyyMMdd-xxxxx` |
| from_warehouse_id | BIGINT | NOT NULL，FK | 调出仓库 | 不可等于调入仓 |
| to_warehouse_id | BIGINT | NOT NULL，FK | 调入仓库 | 不可等于调出仓 |
| transfer_date | DATE | NOT NULL | 调拨日期 | 默认当天 |
| status | VARCHAR(20) | NOT NULL | 状态 | `待确认`、`已确认`、`已取消`；Should 可扩展在途 |
| remark | VARCHAR(255) | NULL | 备注 |  |
| created_by / created_at | BIGINT / DATETIME | NOT NULL | 创建人、创建时间 |  |
| confirmed_by / confirmed_at | BIGINT / DATETIME | NULL | 确认人、确认时间 | 仅已确认有值 |
| version | BIGINT | NOT NULL DEFAULT 0 | 版本号 | 防重复确认 |

索引：`uk_transfer_no (transfer_no)`、`idx_transfer_from_status_date (from_warehouse_id, status, transfer_date)`、`idx_transfer_to_status_date (to_warehouse_id, status, transfer_date)`。

## 表名：wh_transfer_order_item（新增）

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| id | BIGINT | PK，自增 | 明细主键 |  |
| transfer_order_id | BIGINT | NOT NULL，FK | 调拨单主表 | 级联删除仅限待确认单 |
| product_id | BIGINT | NOT NULL，FK | 商品 | 同一单据内唯一 |
| quantity | DECIMAL(18,4) | NOT NULL，> 0 | 调拨数量 | 按商品单位展示 |
| remark | VARCHAR(255) | NULL | 明细备注 |  |

约束：`UNIQUE (transfer_order_id, product_id)`；单据最多 50 行，由服务层校验。

## 表名：wh_stocktake_order（新增）

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| id | BIGINT | PK，自增 | 盘点单主键 |  |
| stocktake_no | VARCHAR(50) | NOT NULL，UNIQUE | 盘点单号 | 格式 `STKyyyyMMdd-xxxxx` |
| warehouse_id | BIGINT | NOT NULL，FK | 盘点仓库 | 单据只对应一个仓库 |
| stocktake_date | DATE | NOT NULL | 盘点日期 | 默认当天 |
| status | VARCHAR(20) | NOT NULL | 状态 | `草稿`、`待确认`、`已确认`、`已取消` |
| remark | VARCHAR(255) | NULL | 盘点说明 |  |
| created_by / created_at | BIGINT / DATETIME | NOT NULL | 创建人、创建时间 |  |
| confirmed_by / confirmed_at | BIGINT / DATETIME | NULL | 确认人、确认时间 |  |
| version | BIGINT | NOT NULL DEFAULT 0 | 版本号 | 防重复确认 |

索引：`uk_stocktake_no (stocktake_no)`、`idx_stocktake_warehouse_status_date (warehouse_id, status, stocktake_date)`。

## 表名：wh_stocktake_order_item（新增）

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| id | BIGINT | PK，自增 | 明细主键 |  |
| stocktake_order_id | BIGINT | NOT NULL，FK | 盘点单主表 |  |
| product_id | BIGINT | NOT NULL，FK | 商品 | 同一单据内唯一 |
| book_quantity | DECIMAL(18,4) | NOT NULL | 创建/刷新时账面数量 | 确认前用于数据变化校验 |
| counted_quantity | DECIMAL(18,4) | NOT NULL，>= 0 | 实盘数量 | 不允许负数 |
| difference_quantity | DECIMAL(18,4) | NOT NULL | 实盘 - 账面 | 服务端计算，前端不可提交为事实 |
| reason | VARCHAR(255) | NOT NULL | 调整原因 | 盘盈/盘亏必须填写 |
| balance_version | BIGINT | NOT NULL | 读取时库存版本 | 确认时与当前版本比对 |

约束：`UNIQUE (stocktake_order_id, product_id)`；盘点至少包含一条明细。

## 表名：sys_data_migration（新增）

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| migration_key | VARCHAR(50) | PK | 迁移标识 | 固定 `SNAPSHOT_TO_RELATIONAL_V3` |
| status | VARCHAR(20) | NOT NULL | 迁移状态 | `未开始`、`进行中`、`成功`、`失败` |
| source_updated_at | DATETIME | NULL | 旧快照版本时间 | 防止迁移期间源数据变更 |
| summary_json | JSON | NULL | 数量、校验和、异常摘要 | 不保存密码等敏感字段 |
| started_at / finished_at | DATETIME | NULL | 起止时间 |  |
| error_message | VARCHAR(1000) | NULL | 失败摘要 | 完整异常写服务器日志 |

## 表名：sys_user（V3 扩展字段，Should）

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| must_change_password | TINYINT | NOT NULL DEFAULT 0 | 是否首次登录改密 | 管理员重置后置 1 |
| failed_login_count | INT | NOT NULL DEFAULT 0 | 连续失败次数 | 成功登录归零 |
| locked_until | DATETIME | NULL | 锁定截止时间 | 超过阈值时设置 |

`sys_operation_log` 保持现有结构，不另建重复审计表。登录成功/失败、登录退出、调拨确认、盘点确认等均以不同 `module`、`action`、`business_no` 写入该表。

# 业务逻辑

## DAT-31 / DAT-32：数据切换与迁移

1. 上线前先停止普通业务写入、备份 `nova_erp`，并校验 Flyway 已成功执行 V1~V7。
2. 后端启动时只允许管理员通过受控运维命令触发一次迁移服务，不向浏览器暴露“开始迁移”接口。
3. 迁移服务读取 `erp_application_state.id = 1` 的旧快照；读取后记录 `source_updated_at`，并在单个数据库事务内依次写入主数据、库存余额、库存流水、采购/销售及其明细。
4. 迁移时保留旧业务编号、业务日期、状态和操作时间；若旧对象 ID 与新表主键不一致，维护内存映射，保证明细、库存流水的来源关系正确。
5. 写入后执行对账：商品、仓库、供应商、客户、各类单据和流水数量相符；每个“商品+仓库”的余额与迁移前快照一致；对账摘要写入 `sys_data_migration.summary_json`。
6. 任一写入或对账失败，整个事务回滚，状态写为“失败”，旧快照仍为唯一运行数据；不得同时让两套数据接受业务写入。
7. 对账成功后状态写为“成功”，配置开关切换为 `RELATIONAL`；所有领域服务通过 Repository 读写关系表，`PersistenceWebConfig` 对 `erp_application_state` 的自动保存逻辑下线。
8. 首次切换后的每一笔采购、销售、退货、调拨、盘点均只写关系表。旧快照表保留至 V3 稳定验收后，作为只读备份依据。

## AUTH-31：登录、退出与登录页

1. `LoginView.vue` 为应用独立首屏。应用初始化时调用 `GET /api/auth/me`：已登录则渲染主应用，未登录则只渲染登录页。
2. 登录页使用 NOVA ERP 标识、居中表单卡片、深蓝渐变背景及低对比度仓储/物流/数据网格抽象元素。该视觉只服务登录入口，不要求改动既有业务页。
3. 表单字段为用户名、密码；密码默认掩码，支持显示/隐藏；Enter 等同点击登录。按钮提交期间禁用，避免重复请求。
4. `POST /api/auth/login` 只接收用户名和密码。后端查询启用用户和启用角色，使用 BCrypt 比对 `password_hash`；成功后创建 Spring Security Session，并设置 `HttpOnly`、`SameSite=Lax` 的会话 Cookie。
5. 登录成功更新 `last_login_at`，写“登录成功”日志，返回当前用户、角色和权限编码；前端跳转经营概览。失败时统一返回“用户名或密码错误”，不透露账号是否存在；写“登录失败”日志但不记录密码。
6. `POST /api/auth/logout` 使当前 Session 失效并清除 Cookie；前端清空内存中的用户和权限，回到登录页。
7. 浏览器只可保存最近用户名和非敏感界面偏好；不得保存或自动回填密码。登录页不显示演示账号或默认密码；本地演示账号仅可记录在 README，正式环境必须以环境变量覆盖初始密码。

## AUTH-32：权限计算与接口鉴权

1. 权限以 `permission_code` 表示，例如 `purchase:order:create`、`purchase:order:approve`、`sales:order:create`、`inventory:transfer:confirm`、`inventory:stocktake:confirm`、`system:user:manage`。
2. 用户登录后由后端根据 `sys_user.role_id → sys_role_permission → sys_permission` 聚合权限；角色或权限停用时不下发对应权限。
3. 前端用权限集合控制菜单和按钮可见性，但不能作为安全边界。前端直接访问无权限模块时显示 403 页面或“无权访问”。
4. 后端 Controller/Service 使用 `@PreAuthorize` 或统一权限拦截器校验。无会话返回 401；已登录但无权限返回 403；两者均不得执行任何写操作。
5. 当前用户以 `CurrentUser` 统一获取，禁止由前端传入 `operatorId` 决定操作人。所有单据、流水、日志操作人均从当前会话填充。

## INV-31：仓库间调拨

1. 仓管在调拨单页面选择调出仓、调入仓、日期和商品明细。两仓相同、停用仓库、停用商品、空明细、数量小于等于 0、超过 50 行均在前后端校验并拒绝。
2. 创建时单据状态为“待确认”，生成单号，写入主表、明细和“创建调拨单”操作日志；待确认单可编辑或取消，已确认单不可编辑。
3. 确认前前端弹出二次确认，清楚显示“从哪个仓库调出、调入哪个仓库、每个商品数量”。
4. 确认服务用 `@Transactional` 执行，并按“商品 ID → 仓库 ID”固定顺序锁定/更新相关 `wh_inventory_balance`，降低并发调拨死锁概率。
5. 对每个明细执行带版本和可用量条件的更新：调出仓 `available_quantity >= quantity` 才允许减少；调入仓增加同等数量。更新行数不是 1 时，抛出“库存已变化或可用库存不足”，整体回滚。
6. 两仓余额更新成功后，为每个商品新增两条流水：调出仓为负数、`business_type=仓库调出`；调入仓为正数、`business_type=仓库调入`；两条流水使用同一 `source_type=TRANSFER`、`source_id`、`source_no`。
7. 最后将调拨单更新为“已确认”，填入确认人和时间，新增确认日志。状态更新必须带 `status=待确认 AND version=旧版本` 条件，保证重复请求不会二次扣减库存。
8. 经营概览风险项点击“去调拨”时，跳转调拨创建页并预填风险商品、主仓为调入仓；用户仍需自行选择有库存的调出仓和数量。

## INV-32：库存盘点与调整

1. 仓管选择一个启用仓库创建盘点单。后端读取该仓的库存余额，带出商品、账面数量和 `balance_version`；前端录入实盘数量和必填原因。
2. 提交待确认时后端重新计算 `difference_quantity = counted_quantity - book_quantity`，不信任前端传入的差异值。
3. 确认盘点时用 `@Transactional` 逐项校验当前余额版本仍等于明细中的 `balance_version`。若任何一项已被入库、出库或调拨改变，整单提示“账面库存已变化，请刷新后重新盘点”，不部分确认。
4. 对有差异的明细，将余额更新为实盘数量；本期无库存预留时 `available_quantity = counted_quantity`。新增一条流水：差异大于 0 为 `盘盈调整`，小于 0 为 `盘亏调整`，变化数量为差异值。
5. 无差异明细不新增库存流水，但保留在盘点单中作为盘点记录。确认完成后更新单据状态、确认人、确认时间并记录操作日志。
6. 已确认盘点单不可编辑、取消或再次确认；需要修正时新建另一张盘点单，不直接修改历史流水。

## INV-33：事务、幂等与并发控制

1. 采购入库、销售出库、销售退货、调拨确认、盘点确认统一通过库存领域服务处理，禁止 Controller 直接更新商品库存字段。
2. 每次库存变动必须在同一事务内完成：单据合法性校验 → 库存余额条件更新 → 库存流水写入 → 单据状态更新 → 审计日志写入。
3. 使用余额 `version` 和 SQL 条件更新实现乐观锁；出库和调出同时增加 `available_quantity >= requestedQuantity` 条件，禁止负库存。
4. 流水表的 `uk_stock_flow_source (source_type, source_id, product_id, business_type)` 作为数据库层第二道幂等保护；若重复请求导致唯一键冲突，事务回滚并返回“单据已确认”。
5. 前端按钮在请求中禁用只是体验措施，不能替代后端幂等。所有库存异常以业务信息返回，不把 SQL、堆栈或数据库密码暴露给用户。

## OPS-31 / RPT-31：审计、追溯与统一报表

1. `OperationLogService` 是所有关键操作的唯一入口，写入 `module`、`action`、业务类型/编号、操作人、时间及不含敏感信息的前后摘要。
2. 库存流水查询返回来源类型、来源单号、操作人；前端可按来源类型跳转采购入库、销售出库、退货、调拨或盘点详情。
3. 商品档案总库存通过 `SUM(wh_inventory_balance.quantity)` 计算；库存余额按仓库显示；经营概览主仓预警以主仓 `available_quantity < safety_stock` 判断，并返回总库存、主仓可用、安全库存和缺口。
4. 采购、销售、仓储、报表、经营概览均使用相同的 Repository/查询服务，不在 Vue 页面中重新聚合业务单据。
5. 登录、权限变更、调拨、盘点的成功与失败均应有可查询日志；日志查询仅对管理员和经营管理者开放。

# 相关接口

## AUTH-31：认证接口

| 对应的功能 | 登录 |
| ---------- | ---- |
| path | `POST /api/auth/login` |
| 参数 | `{ "username": "string", "password": "string" }` |
| 返回 | HTTP 200：`{ user: { id, username, realName, role }, permissions: ["..."] }` 并写入 HttpOnly Session Cookie；账号/密码错误或用户停用返回 HTTP 401；不得返回密码、哈希或会话标识 |

| 对应的功能 | 当前登录用户 |
| ---------- | ---- |
| path | `GET /api/auth/me` |
| 参数 | 无；浏览器自动携带会话 Cookie |
| 返回 | HTTP 200：当前用户、角色、权限；未登录返回 HTTP 401 |

| 对应的功能 | 退出登录 |
| ---------- | ---- |
| path | `POST /api/auth/logout` |
| 参数 | 无 |
| 返回 | HTTP 204；服务端会话失效并清除 Cookie |

## INV-31：调拨接口

| 对应的功能 | 创建调拨单 |
| ---------- | ---- |
| path | `POST /api/inventory/transfers` |
| 参数 | `{ fromWarehouseId, toWarehouseId, transferDate, remark, items: [{ productId, quantity, remark }] }` |
| 返回 | HTTP 201：调拨单详情；调出/调入仓相同、商品停用、数量非法等返回 HTTP 400；无权限返回 403 |

| 对应的功能 | 调拨单列表与详情 |
| ---------- | ---- |
| path | `GET /api/inventory/transfers`、`GET /api/inventory/transfers/{id}` |
| 参数 | 列表支持 `keyword`、`fromWarehouseId`、`toWarehouseId`、`status`、`dateFrom`、`dateTo`、`page`、`size` |
| 返回 | HTTP 200：分页列表或含明细、创建/确认信息、状态的详情；无权仓库数据不返回 |

| 对应的功能 | 确认调拨 |
| ---------- | ---- |
| path | `POST /api/inventory/transfers/{id}/confirm` |
| 参数 | `{ "version": number }`；版本来自单据详情 |
| 返回 | HTTP 200：已确认调拨单、受影响仓库余额摘要；库存不足/版本冲突返回 HTTP 409；单据状态不允许返回 HTTP 400；重复确认返回 HTTP 409 或幂等已确认结果 |

| 对应的功能 | 取消待确认调拨单 |
| ---------- | ---- |
| path | `POST /api/inventory/transfers/{id}/cancel` |
| 参数 | `{ "version": number }` |
| 返回 | HTTP 200：已取消单据；仅 `待确认` 可取消，已确认单据返回 HTTP 400 |

## INV-32：盘点接口

| 对应的功能 | 读取仓库账面库存 |
| ---------- | ---- |
| path | `GET /api/inventory/stocktakes/draft-items` |
| 参数 | `warehouseId` 必填；可选 `keyword` |
| 返回 | HTTP 200：商品、账面数量、余额版本；仅仓管/管理员可访问 |

| 对应的功能 | 创建盘点单 |
| ---------- | ---- |
| path | `POST /api/inventory/stocktakes` |
| 参数 | `{ warehouseId, stocktakeDate, remark, items: [{ productId, bookQuantity, balanceVersion, countedQuantity, reason }] }` |
| 返回 | HTTP 201：草稿或待确认盘点单详情；商品重复、实盘负数、空原因等返回 HTTP 400 |

| 对应的功能 | 盘点列表与详情 |
| ---------- | ---- |
| path | `GET /api/inventory/stocktakes`、`GET /api/inventory/stocktakes/{id}` |
| 参数 | 列表支持 `warehouseId`、`status`、`dateFrom`、`dateTo`、`page`、`size` |
| 返回 | HTTP 200：分页列表或包含账面/实盘/差异/原因/确认信息的详情 |

| 对应的功能 | 确认盘点 |
| ---------- | ---- |
| path | `POST /api/inventory/stocktakes/{id}/confirm` |
| 参数 | `{ "version": number }` |
| 返回 | HTTP 200：已确认单据及调整摘要；账面数据变化、版本冲突返回 HTTP 409；无权限返回 HTTP 403 |

## OPS-31 / RPT-31：追溯与报表接口

| 对应的功能 | 查询操作日志 |
| ---------- | ---- |
| path | `GET /api/system/operation-logs` |
| 参数 | `module`、`action`、`businessNo`、`operatorId`、`dateFrom`、`dateTo`、`page`、`size` 均可选 |
| 返回 | HTTP 200：管理员/经营管理者可见的分页日志；无权限返回 HTTP 403 |

| 对应的功能 | 查询库存流水 |
| ---------- | ---- |
| path | `GET /api/warehouse/stock-flows` |
| 参数 | 支持既有筛选并新增 `sourceType=TRANSFER|STOCKTAKE` |
| 返回 | HTTP 200：每行包含来源类型、来源单号、操作人和可跳转标识；数据来自 `wh_stock_flow` |

## 验证要求

1. 后端为每个确认型服务编写集成测试：正常确认、库存不足、版本冲突、重复请求、事务异常回滚。
2. 使用两个独立账号验证 401、403、菜单隐藏和接口拦截；不得只验证前端按钮不可见。
3. 用“黑色签字笔：主仓 18、华东仓 20、安全库存 30、总库存 38”执行华东仓向主仓调拨 12 支，验证两仓余额、总库存、风险卡、两条流水和审计日志。
4. 创建账面 30、实盘 27 的盘点单，验证盘亏流水 -3、库存预警更新、历史不可编辑和操作人可追溯。
5. 完成迁移、重启服务后，抽样核对迁移前后业务编号、单据明细、库存余额、库存流水和报表金额；迁移失败必须可通过数据库备份恢复。
