# ERP V2 持久化开发文档

| 项目 | 内容 |
| --- | --- |
| 对应 PRD | [V2_PRD.md](V2_PRD.md) 中 INF-01、INF-02、MDM-01~04、PUR-21~22、SAL-21~23、INV-21、SYS-21、OPS-01 |
| 文档版本 | V2.0（开发评审稿） |
| 实施范围 | MySQL 持久化、统一主数据、库存事务、系统设置落库、审计基础能力 |
| 不在本次实现 | 财务、批次库位、复杂审批流、第三方集成 |
| 技术基线 | JDK 17、Spring Boot 3.4、MySQL 8、Vue 3 |

# 业务背景

V1 已完成商品、采购、销售、仓储、业务报表和系统设置的页面及核心演示闭环。但商品、采购、销售、仓储和报表后端数据目前主要保存在 Java 内存；系统设置数据保存在浏览器 localStorage。服务或浏览器重启后，数据不能作为正式业务依据继续使用。

V2 持久化改造的目的，是让所有业务模块改用同一个 MySQL 数据库作为事实来源：

1. 基础资料由后端统一保存，采购、销售、仓储不再使用各自的演示副本。
2. 采购入库、销售出库、销售退货通过数据库事务更新库存余额、库存流水和单据状态。
3. 用户、角色、权限和操作日志落库，为后续登录鉴权提供数据基础。
4. 报表直接从已确认单据和库存流水统计，服务重启后结果保持一致。

本开发文档只描述“从内存/本地缓存迁移到 MySQL”的实现方案。现有 V1 页面结构尽量保留；接口优先保持现有请求字段和返回字段兼容，避免前端大范围重写。

# 功能梳理

| 编号 | 功能 | 当前问题 | V2 处理方式 | 优先级 |
| --- | --- | --- | --- | --- |
| PER-01 | 数据库初始化 | 新成员没有统一表结构和演示数据 | 使用 Flyway 版本化 SQL 建表、建索引、初始化角色与管理员 | Must |
| PER-02 | 数据访问层 | Service 中使用 `List`、`Map` 保存业务数据 | 引入 Spring Data JPA，实体、Repository、Service 分层 | Must |
| PER-03 | 商品与主数据 | 商品、仓库、供应商、客户存在模块内演示副本 | 统一为 `md_*` 表及公共查询接口 | Must |
| PER-04 | 采购单据 | 采购订单和入库单重启后丢失 | 采购订单、明细、入库单、明细落库 | Must |
| PER-05 | 销售单据 | 销售订单、出库单、退货单重启后丢失 | 销售订单、明细、出库单、退货单及明细落库 | Must |
| PER-06 | 库存事务 | 库存余额和流水只在内存中变动 | 以“商品 + 仓库”库存余额表和不可修改流水表实现 | Must |
| PER-07 | 幂等与并发 | 重复确认可能重复增减库存；并发下可能超卖 | 单据状态校验、唯一约束、乐观锁版本号和事务回滚 | Must |
| PER-08 | 系统设置 | 用户、角色、权限数据仅存在浏览器 | 用户、角色、权限、角色权限关系落库；前端逐步替换 localStorage Mock | Must |
| PER-09 | 操作日志 | 关键操作无法追溯操作者和时间 | 写入登录、审核、确认、主数据启停、授权变更日志 | Must |
| PER-10 | 报表数据源 | 报表读取内存服务 | 统一从 MySQL 已确认单据和库存流水查询 | Must |
| PER-11 | 初始化/迁移 | V1 演示数据无保留方案 | 提供可选种子数据脚本；不直接迁移浏览器 localStorage | Should |

## 实施边界

1. 数据库名称建议为 `nova_erp`，字符集统一使用 `utf8mb4`，排序规则使用 `utf8mb4_0900_ai_ci`（低版本 MySQL 可改为 `utf8mb4_general_ci`）。
2. 所有业务表使用 `BIGINT` 主键；金额使用 `DECIMAL(18,2)`；数量使用 `DECIMAL(18,4)`，以兼容“件、箱、公斤”等非整数单位。
3. 业务单据不做物理删除。主数据仅允许启用/停用；历史单据通过快照字段保留当时的名称、编码和价格。
4. 默认时区为 `Asia/Shanghai`；后端时间使用 `LocalDateTime`，数据库使用 `DATETIME`。
5. 数据库连接密码只能放在本机 `application-local.yml` 或环境变量中，严禁提交到 Git。

# 数据库设计

## 1. 命名与公共字段约定

| 约定 | 规则 |
| --- | --- |
| 主键 | `id BIGINT UNSIGNED AUTO_INCREMENT` |
| 状态 | 主数据使用 `status TINYINT`：`1=启用，0=停用`；单据使用 `VARCHAR(20)` 状态码 |
| 审计字段 | 业务/主数据表统一保留 `created_at`、`created_by`、`updated_at`、`updated_by` |
| 单号 | `*_no` 唯一，例如 `PO20260906-0001`、`SO20260906-0001` |
| 金额 | `DECIMAL(18,2)`，不使用 `double/float` |
| 数量 | `DECIMAL(18,4)`，前端仍可按业务单位展示 |
| 逻辑删除 | V2 暂不引入通用删除字段；已被业务单据引用的主数据不可物理删除 |

## 2. 系统与权限表

表名：`sys_user`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 用户 ID | 自增 |
| username | VARCHAR(50) | NOT NULL, UNIQUE | 登录账号 | 创建后不可修改 |
| password_hash | VARCHAR(100) | NOT NULL | BCrypt 密码哈希 | 永不返回前端 |
| real_name | VARCHAR(50) |  | 姓名 |  |
| role_id | BIGINT | NOT NULL, FK | 角色 ID | V2 一名用户一个主角色 |
| status | TINYINT | NOT NULL DEFAULT 1 | 启停状态 | 停用不可登录 |
| last_login_at | DATETIME |  | 最后登录时间 |  |
| created_at / created_by | DATETIME / BIGINT | NOT NULL | 创建审计 |  |
| updated_at / updated_by | DATETIME / BIGINT | NOT NULL | 更新审计 |  |

表名：`sys_role`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 角色 ID | 自增 |
| role_code | VARCHAR(50) | NOT NULL, UNIQUE | 角色编码 | 如 `admin`、`purchaser` |
| role_name | VARCHAR(50) | NOT NULL | 角色名称 |  |
| status | TINYINT | NOT NULL DEFAULT 1 | 启停状态 | `admin` 不允许停用 |
| remark | VARCHAR(255) |  | 备注 |  |
| created_at / created_by | DATETIME / BIGINT | NOT NULL | 创建审计 |  |
| updated_at / updated_by | DATETIME / BIGINT | NOT NULL | 更新审计 |  |

表名：`sys_permission`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 权限 ID | 自增 |
| parent_id | BIGINT | NOT NULL DEFAULT 0 | 父菜单 ID | 用于菜单树 |
| name | VARCHAR(80) | NOT NULL | 菜单/按钮名称 |  |
| permission_code | VARCHAR(100) | UNIQUE | 权限码 | 如 `system:user:add` |
| type | VARCHAR(10) | NOT NULL | 类型 | `MENU` / `BUTTON` |
| path | VARCHAR(150) |  | 前端路由/菜单路径 |  |
| sort_no | INT | NOT NULL DEFAULT 0 | 排序 |  |
| status | TINYINT | NOT NULL DEFAULT 1 | 启停状态 |  |

表名：`sys_role_permission`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| role_id | BIGINT | PK, FK | 角色 ID | 联合主键之一 |
| permission_id | BIGINT | PK, FK | 权限 ID | 联合主键之一 |
| created_at | DATETIME | NOT NULL | 授权时间 |  |
| created_by | BIGINT | NOT NULL | 授权人 |  |

表名：`sys_operation_log`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 日志 ID | 自增 |
| module | VARCHAR(40) | NOT NULL | 模块 | 商品、采购、销售、仓储、系统设置等 |
| action | VARCHAR(40) | NOT NULL | 动作 | CREATE、SUBMIT、APPROVE、CONFIRM 等 |
| business_type | VARCHAR(40) |  | 业务对象类型 |  |
| business_id | BIGINT |  | 业务对象 ID |  |
| business_no | VARCHAR(50) |  | 业务单号 |  |
| before_summary | JSON |  | 操作前摘要 | 不写密码/哈希 |
| after_summary | JSON |  | 操作后摘要 | 不写密码/哈希 |
| operator_id | BIGINT |  | 操作用户 | 未登录失败日志允许为空 |
| operator_name | VARCHAR(50) |  | 操作人快照 |  |
| ip_address | VARCHAR(45) |  | IP 地址 | 可获取时记录 |
| created_at | DATETIME | NOT NULL | 操作时间 | 建索引 |

## 3. 基础主数据表

表名：`md_product`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 商品 ID | 自增 |
| sku | VARCHAR(32) | NOT NULL, UNIQUE | 商品编码 | 保持 V1 字段命名 |
| name | VARCHAR(80) | NOT NULL | 商品名称 |  |
| category | VARCHAR(40) | NOT NULL | 分类 | V2 暂不拆分类表 |
| unit | VARCHAR(20) | NOT NULL | 单位 |  |
| price | DECIMAL(18,2) | NOT NULL | 参考销售价 | 不作为财务成本 |
| safety_stock | DECIMAL(18,4) | NOT NULL DEFAULT 0 | 安全库存 | 全仓汇总口径 |
| status | TINYINT | NOT NULL DEFAULT 1 | 启停状态 |  |
| created_at / created_by | DATETIME / BIGINT | NOT NULL | 创建审计 |  |
| updated_at / updated_by | DATETIME / BIGINT | NOT NULL | 更新审计 |  |

表名：`md_warehouse`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 仓库 ID | 自增 |
| code | VARCHAR(32) | NOT NULL, UNIQUE | 仓库编码 |  |
| name | VARCHAR(80) | NOT NULL | 仓库名称 |  |
| manager | VARCHAR(50) |  | 负责人 |  |
| address | VARCHAR(200) |  | 地址 |  |
| status | TINYINT | NOT NULL DEFAULT 1 | 启停状态 |  |
| remark | VARCHAR(255) |  | 备注 |  |
| created_at / created_by | DATETIME / BIGINT | NOT NULL | 创建审计 |  |
| updated_at / updated_by | DATETIME / BIGINT | NOT NULL | 更新审计 |  |

表名：`md_supplier`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 供应商 ID | 自增 |
| code | VARCHAR(32) | NOT NULL, UNIQUE | 供应商编码 |  |
| name | VARCHAR(100) | NOT NULL | 供应商名称 |  |
| contact | VARCHAR(50) |  | 联系人 |  |
| phone | VARCHAR(30) |  | 电话 |  |
| status | TINYINT | NOT NULL DEFAULT 1 | 启停状态 |  |
| remark | VARCHAR(255) |  | 备注 |  |
| created_at / created_by | DATETIME / BIGINT | NOT NULL | 创建审计 |  |
| updated_at / updated_by | DATETIME / BIGINT | NOT NULL | 更新审计 |  |

表名：`md_customer`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 客户 ID | 自增 |
| code | VARCHAR(32) | NOT NULL, UNIQUE | 客户编码 |  |
| name | VARCHAR(100) | NOT NULL | 客户名称 |  |
| contact | VARCHAR(50) |  | 联系人 |  |
| phone | VARCHAR(30) |  | 电话 |  |
| status | TINYINT | NOT NULL DEFAULT 1 | 启停状态 |  |
| remark | VARCHAR(255) |  | 备注 |  |
| created_at / created_by | DATETIME / BIGINT | NOT NULL | 创建审计 |  |
| updated_at / updated_by | DATETIME / BIGINT | NOT NULL | 更新审计 |  |

## 4. 采购与入库表

表名：`pur_purchase_order`、`pur_purchase_order_item`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| pur_purchase_order.id | BIGINT | PK | 采购订单 ID |  |
| order_no | VARCHAR(50) | NOT NULL, UNIQUE | 采购单号 |  |
| supplier_id / supplier_name | BIGINT / VARCHAR(100) | NOT NULL | 供应商与名称快照 | 历史可读 |
| order_date / expected_arrival_date | DATE / DATE | NOT NULL / 可空 | 订单、预计到货日期 |  |
| status | VARCHAR(20) | NOT NULL | 单据状态 | 草稿/待审核/已审核/已完成/已驳回/已作废 |
| total_quantity / total_amount | DECIMAL(18,4) / DECIMAL(18,2) | NOT NULL | 汇总数值 | 冗余便于查询 |
| received_quantity | DECIMAL(18,4) | NOT NULL DEFAULT 0 | 累计实收数量 |  |
| remark / reject_reason | VARCHAR(255) | 可空 | 备注、驳回原因 |  |
| submitted_at / approved_at / approved_by | DATETIME / DATETIME / BIGINT | 可空 | 流程审计 |  |
| pur_purchase_order_item.id | BIGINT | PK | 采购明细 ID |  |
| purchase_order_id | BIGINT | NOT NULL, FK | 采购订单 ID | 建索引 |
| product_id / sku / product_name / unit | BIGINT / VARCHAR / VARCHAR / VARCHAR | NOT NULL | 商品快照 |  |
| ordered_quantity / received_quantity | DECIMAL(18,4) | NOT NULL | 订购/累计实收数量 |  |
| unit_price / line_amount | DECIMAL(18,2) | NOT NULL | 单价、小计 |  |

表名：`wh_purchase_receipt`、`wh_purchase_receipt_item`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| wh_purchase_receipt.id | BIGINT | PK | 入库单 ID |  |
| receipt_no | VARCHAR(50) | NOT NULL, UNIQUE | 入库单号 |  |
| purchase_order_id / purchase_order_no | BIGINT / VARCHAR(50) | NOT NULL | 来源采购订单 |  |
| warehouse_id / warehouse_name | BIGINT / VARCHAR(80) | NOT NULL | 入库仓库快照 |  |
| stock_in_date | DATE | NOT NULL | 入库日期 |  |
| status | VARCHAR(20) | NOT NULL | 草稿/已确认 | 确认后不可修改 |
| total_quantity | DECIMAL(18,4) | NOT NULL | 本次实收总数 |  |
| confirmed_at / confirmed_by | DATETIME / BIGINT | 可空 | 确认审计 |  |
| wh_purchase_receipt_item.id | BIGINT | PK | 入库明细 ID |  |
| purchase_receipt_id | BIGINT | NOT NULL, FK | 入库单 ID | 建索引 |
| purchase_order_item_id | BIGINT | NOT NULL, FK | 来源采购明细 |  |
| product_id / product_name / unit | BIGINT / VARCHAR / VARCHAR | NOT NULL | 商品快照 |  |
| received_quantity | DECIMAL(18,4) | NOT NULL | 本次实收数量 | 大于 0 |

## 5. 销售、出库与退货表

表名：`sal_sales_order`、`sal_sales_order_item`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| sal_sales_order.id | BIGINT | PK | 销售订单 ID |  |
| order_no | VARCHAR(50) | NOT NULL, UNIQUE | 销售单号 |  |
| customer_id / customer_name | BIGINT / VARCHAR(100) | NOT NULL | 客户快照 |  |
| warehouse_id / warehouse_name | BIGINT / VARCHAR(80) | NOT NULL | 发货仓库快照 |  |
| order_date / required_ship_date | DATE / DATE | NOT NULL / 可空 | 订单、要求发货日期 |  |
| status | VARCHAR(20) | NOT NULL | 单据状态 |  |
| total_quantity / shipped_quantity | DECIMAL(18,4) | NOT NULL | 订单/累计出库数量 |  |
| total_amount | DECIMAL(18,2) | NOT NULL | 订单金额 |  |
| remark / reject_reason | VARCHAR(255) | 可空 | 备注、驳回原因 |  |
| sal_sales_order_item.id | BIGINT | PK | 销售明细 ID |  |
| sales_order_id | BIGINT | NOT NULL, FK | 销售订单 ID | 建索引 |
| product_id / sku / product_name / unit | BIGINT / VARCHAR / VARCHAR / VARCHAR | NOT NULL | 商品快照 |  |
| ordered_quantity / shipped_quantity | DECIMAL(18,4) | NOT NULL | 下单/累计出库数量 |  |
| unit_price / line_amount | DECIMAL(18,2) | NOT NULL | 成交单价、小计 |  |

表名：`wh_stock_out_order`、`wh_stock_out_order_item`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| wh_stock_out_order.id | BIGINT | PK | 出库单 ID |  |
| stock_out_no | VARCHAR(50) | NOT NULL, UNIQUE | 出库单号 |  |
| sales_order_id / sales_order_no | BIGINT / VARCHAR(50) | NOT NULL | 来源销售订单 |  |
| customer_id / customer_name | BIGINT / VARCHAR(100) | NOT NULL | 客户快照 |  |
| warehouse_id / warehouse_name | BIGINT / VARCHAR(80) | NOT NULL | 出库仓库快照 |  |
| stock_out_date | DATE | NOT NULL | 出库日期 |  |
| status | VARCHAR(20) | NOT NULL | 草稿/已确认 | 确认后不可修改 |
| total_quantity | DECIMAL(18,4) | NOT NULL | 本次出库总数 |  |
| confirmed_at / confirmed_by | DATETIME / BIGINT | 可空 | 确认审计 |  |
| wh_stock_out_order_item.id | BIGINT | PK | 出库明细 ID |  |
| stock_out_order_id | BIGINT | NOT NULL, FK | 出库单 ID | 建索引 |
| sales_order_item_id | BIGINT | NOT NULL, FK | 来源销售明细 |  |
| product_id / product_name / unit | BIGINT / VARCHAR / VARCHAR | NOT NULL | 商品快照 |  |
| shipped_quantity | DECIMAL(18,4) | NOT NULL | 本次出库数量 | 大于 0 |
| returned_quantity | DECIMAL(18,4) | NOT NULL DEFAULT 0 | 已退货数量 | 用于可退校验 |

表名：`sal_sales_return`、`sal_sales_return_item`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| sal_sales_return.id | BIGINT | PK | 退货单 ID |  |
| return_no | VARCHAR(50) | NOT NULL, UNIQUE | 退货单号 |  |
| source_stock_out_id / source_stock_out_no | BIGINT / VARCHAR(50) | NOT NULL | 来源出库单 |  |
| sales_order_id / sales_order_no | BIGINT / VARCHAR(50) | NOT NULL | 来源销售订单 |  |
| customer_id / customer_name | BIGINT / VARCHAR(100) | NOT NULL | 客户快照 |  |
| warehouse_id / warehouse_name | BIGINT / VARCHAR(80) | NOT NULL | 回库仓库快照 |  |
| return_date | DATE | NOT NULL | 退货日期 |  |
| status | VARCHAR(20) | NOT NULL | 草稿/待审核/已审核/已确认 |  |
| reason / remark | VARCHAR(255) | 可空 | 原因、备注 |  |
| total_quantity | DECIMAL(18,4) | NOT NULL | 退货总数 |  |
| sal_sales_return_item.id | BIGINT | PK | 退货明细 ID |  |
| sales_return_id | BIGINT | NOT NULL, FK | 退货单 ID | 建索引 |
| stock_out_order_item_id | BIGINT | NOT NULL, FK | 来源出库明细 |  |
| product_id / product_name / unit | BIGINT / VARCHAR / VARCHAR | NOT NULL | 商品快照 |  |
| returned_quantity | DECIMAL(18,4) | NOT NULL | 本次退货数量 | 不超过可退数量 |

## 6. 库存余额与流水表

表名：`wh_inventory_balance`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 库存余额 ID | 自增 |
| warehouse_id | BIGINT | NOT NULL, FK | 仓库 ID |  |
| product_id | BIGINT | NOT NULL, FK | 商品 ID |  |
| quantity | DECIMAL(18,4) | NOT NULL DEFAULT 0 | 现存数量 | 不允许小于 0 |
| locked_quantity | DECIMAL(18,4) | NOT NULL DEFAULT 0 | 锁定数量 | V2 初始固定 0 |
| available_quantity | DECIMAL(18,4) | NOT NULL DEFAULT 0 | 可用数量 | `quantity - locked_quantity` |
| version | BIGINT | NOT NULL DEFAULT 0 | 乐观锁版本 | 更新时递增 |
| updated_at / updated_by | DATETIME / BIGINT | NOT NULL | 最近变动审计 |  |

唯一约束：`uk_inventory_warehouse_product (warehouse_id, product_id)`。

表名：`wh_stock_flow`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| --- | --- | --- | --- | --- |
| id | BIGINT | PK | 流水 ID | 自增 |
| flow_no | VARCHAR(50) | NOT NULL, UNIQUE | 流水号 |  |
| warehouse_id / warehouse_name | BIGINT / VARCHAR(80) | NOT NULL | 仓库快照 |  |
| product_id / sku / product_name / unit | BIGINT / VARCHAR / VARCHAR / VARCHAR | NOT NULL | 商品快照 |  |
| business_type | VARCHAR(30) | NOT NULL | 业务类型 | 采购入库、销售出库、销售退货、库存调整 |
| change_quantity | DECIMAL(18,4) | NOT NULL | 变动量 | 入库为正，出库为负 |
| before_quantity / after_quantity | DECIMAL(18,4) | NOT NULL | 变动前后数量 | 便于审计 |
| source_type / source_id / source_no | VARCHAR / BIGINT / VARCHAR | NOT NULL | 来源单据 | 建组合索引 |
| remark | VARCHAR(255) |  | 备注 |  |
| operator_id / operator_name | BIGINT / VARCHAR(50) |  | 操作人快照 |  |
| created_at | DATETIME | NOT NULL | 发生时间 | 建索引 |

## 7. 索引与外键策略

| 表 | 索引/约束 | 用途 |
| --- | --- | --- |
| 所有主数据表 | 编码唯一索引 | 防止重复商品、仓库、供应商、客户 |
| 所有单据头表 | 单号唯一索引、状态+日期索引 | 单号查询、列表筛选、报表统计 |
| 所有单据明细表 | 来源头表 ID 索引、商品 ID 索引 | 单据详情和商品维度统计 |
| wh_inventory_balance | 仓库+商品唯一索引、版本号 | 余额唯一和乐观锁更新 |
| wh_stock_flow | 商品+仓库+时间索引、来源类型+来源 ID 索引 | 流水查询和追溯 |
| sys_operation_log | 业务编号+时间索引、操作人+时间索引 | 审计检索 |

外键采用“数据库外键 + 服务层校验”的组合：明细表和库存余额建立外键以避免孤儿记录；对可能影响历史单据的主数据不做物理删除。若课程环境不便使用外键，可保留相同字段与服务层校验，但必须保留对应索引。

# 业务逻辑

## 1. 初始化与配置（PER-01）

1. `pom.xml` 增加 `spring-boot-starter-data-jpa`、`mysql-connector-j`、`flyway-core`、`spring-security-crypto` 依赖。
2. 新增 `application-local.yml.example`，只提供变量名：`DB_HOST`、`DB_PORT`、`DB_NAME`、`DB_USERNAME`、`DB_PASSWORD`，不保存真实密码。
3. Flyway 脚本放置在 `src/main/resources/db/migration`：
   - `V1__create_schema.sql`：创建全部表和索引。
   - `V2__seed_roles_permissions.sql`：初始化角色、权限、管理员密码哈希。
   - 后续表变更必须新增版本文件，不修改已经执行过的脚本。
4. 后端启动时使用 `spring.jpa.hibernate.ddl-auto=validate`：只验证实体与表是否一致，不允许 Hibernate 自动改表。
5. 开发环境首次运行前由开发者创建空数据库 `nova_erp`；应用负责建表和种子数据。

## 2. 内存服务替换策略（PER-02）

1. 现有 `ProductService`、`PurchaseService`、`SalesService`、`WarehouseService`、`ReportService` 保留业务职责，不再以字段 `List`/`Map` 保存正式状态。
2. 每个领域新增 JPA Entity 和 Repository；Controller 继续使用 Request/Response DTO，不直接暴露 Entity。
3. 迁移顺序：主数据 → 库存余额/流水 → 采购 → 销售 → 报表 → 系统设置/登录。每一步构建通过后再迁移下一步。
4. V1 当前 API 的 URL 与主要 JSON 字段优先保持不变；新增分页时采用 `page`、`size`、`sort` 查询参数，老页面未传时使用默认值。
5. 迁移完成后删除或隔离内存种子逻辑，避免同一接口出现“数据库与内存混读”。

## 3. 主数据统一逻辑（PER-03）

1. 商品、仓库、供应商、客户新增/编辑/启停全部写入 `md_*` 表，并记录 `sys_operation_log`。
2. 新建采购单只能选择 `md_supplier.status=1` 与 `md_product.status=1` 的资料。
3. 新建销售单只能选择 `md_customer.status=1`、`md_product.status=1`、`md_warehouse.status=1` 的资料。
4. 主数据被历史单据引用后禁止物理删除；前端只显示“停用”。
5. 商品页面的库存显示由 `wh_inventory_balance` 汇总得出；编辑商品时不接收库存修改字段。

## 4. 采购订单与入库事务（PER-04）

1. 创建采购订单时，在一个事务中写入订单头和明细；订单金额、总数量由后端根据明细重新计算。
2. 提交、审核、驳回采用受控状态机：
   - 草稿/已驳回 → 待审核；
   - 待审核 → 已审核或已驳回；
   - 已审核且全部收货 → 已完成。
3. 创建采购入库单时校验来源订单状态为“已审核/部分收货”，且每条明细本次实收数量大于 0、不超过未收数量。
4. 确认采购入库必须在一个 `@Transactional` 方法中依次完成：锁定入库单与订单明细 → 校验未收数量 → 更新入库单状态 → 更新订单明细/订单累计实收数量 → 增加库存余额 → 写采购入库流水 → 写操作日志。
5. 已确认入库单再次确认时直接返回业务错误，不允许再次增加库存。

## 5. 销售出库与退货事务（PER-05）

1. 创建销售订单时，商品、客户、仓库必须为启用状态；订单金额由后端按明细计算。
2. 确认销售出库前，按“仓库 + 商品”读取库存余额并执行乐观锁更新：
   - `available_quantity >= shipped_quantity` 才允许扣减；
   - 更新条件包含 `version`，更新成功后版本加 1；
   - 任一明细库存不足或版本冲突，整个事务回滚并提示具体商品和可用量。
3. 确认成功后同步更新出库单状态、销售订单已出库量、销售订单状态、库存余额、库存流水、操作日志。
4. 创建销售退货时校验来源出库单已确认，且“累计已退数量 + 本次退货数量 ≤ 原出库数量”。
5. 确认销售退货在一个事务中增加原仓库存、更新来源出库明细的已退数量、确认退货单、写退货流水和操作日志。

## 6. 库存余额与流水（PER-06、PER-07）

1. `InventoryService` 是唯一允许更新 `wh_inventory_balance` 的服务；商品、采购、销售 Controller 不得自行执行库存 SQL。
2. 所有变动统一调用 `changeStock(command)`，参数包含仓库、商品、变动数量、业务类型、来源单据、操作人。
3. `changeStock(command)` 逻辑：读取/必要时创建余额行 → 校验数量和可用量 → 乐观锁更新余额 → 写入一条流水（含变动前后数）→ 返回最新余额。
4. 对同一来源单据的确认操作，业务单据状态和 `source_type + source_id + business_type` 必须防止重复写流水。
5. 库存流水不提供修改、删除 API；若后续发生业务逆转，必须新增反向流水，而非改写历史流水。

## 7. 系统设置、登录与审计（PER-08、PER-09）

1. 后端新增认证模块：账号密码校验成功后返回短期访问令牌；密码使用 BCrypt 哈希校验。
2. 当前用户接口从令牌解析用户、角色与权限码；停用用户或停用角色立即拒绝访问。
3. 用户、角色、权限配置以数据库为准；V1 `SystemSettingsView.vue` 的 localStorage 仅在未对接阶段保留，完成后移除 Mock 数据写入。
4. 控制器或服务层使用权限注解/统一拦截器校验，例如 `system:user:add`、`purchase:order:approve`、`sales:stockout:confirm`。
5. 登录、创建/提交/审核/确认单据、启停主数据、变更角色权限均写操作日志；日志写入失败应使关键业务事务回滚，普通查询日志失败不影响查询。

## 8. 报表查询改造（PER-10）

1. 报表只统计已确认入库、已确认出库、已确认退货及有效库存流水；草稿、待审核、驳回数据不得计入。
2. 报表查询使用只读事务，并支持日期、仓库、商品、供应商、客户等条件。
3. 库存余额报表以 `wh_inventory_balance` 为主，关联商品和仓库；收发存统计使用库存流水按日期聚合。
4. CSV 导出复用与列表相同的查询条件，后端流式输出 UTF-8 BOM CSV，避免前端一次性生成大量数据。

## 9. 数据初始化与回滚策略（PER-11）

1. V1 内存演示数据不能自动视为生产数据；首次接入数据库时通过 Flyway 种子脚本生成等价演示数据。
2. 浏览器 localStorage 的系统设置数据不自动导入数据库，避免把个人浏览器的临时数据误当正式数据。
3. 每一个数据库变更使用新的 Flyway 版本脚本；若功能发布失败，采用“新脚本补偿”或应用版本回退，不手工删除已执行迁移记录。
4. 开发阶段可使用专用测试库反复清空；测试和演示库清空前必须明确目标数据库名称并获得执行人确认。

# 相关接口

统一约定：成功响应为 `200`；参数校验失败为 `400`；未登录为 `401`；无权限为 `403`；资源不存在为 `404`；编码/单号重复或状态冲突为 `409`。错误响应包含可展示的 `message` 和可选 `fieldErrors`。

## 1. 主数据接口

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET /api/products?page=&size=&keyword=&status=` |
| 参数 | 页码、每页数量、商品关键字、状态；不传分页时按现有前端兼容规则返回 |
| 返回 | 商品列表/分页结果；库存字段由库存余额汇总，只读 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `POST /api/products`、`PUT /api/products/{id}`、`POST /api/products/{id}/status` |
| 参数 | 商品编码、名称、分类、单位、售价、安全库存、状态；更新时不接收库存修改 |
| 返回 | 保存后的商品数据；编码重复返回 409 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET/POST/PUT /api/warehouses`、`/api/suppliers`、`/api/customers` |
| 参数 | 对应编码、名称、联系人/负责人、电话/地址、状态、备注；列表支持分页筛选 |
| 返回 | 统一主数据列表或保存结果；停用历史资料仍可被单据详情查询 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET /api/warehouses/enabled`、`/api/suppliers/enabled`、`/api/customers/enabled`、`/api/products/enabled` |
| 参数 | 无或关键字 | 返回所有可用于新单据的启用资料 |

## 2. 认证与系统设置接口

| 对应的功能 | 内容 |
| --- | --- |
| path | `POST /api/auth/login` |
| 参数 | `username`、`password` |
| 返回 | `accessToken`、用户基本信息、角色、权限码数组；不返回密码哈希 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET /api/auth/me`、`POST /api/auth/logout` |
| 参数 | 请求头 `Authorization: Bearer {token}` |
| 返回 | 当前用户、角色、权限或退出成功结果 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET/POST/PUT /api/system/users`、`POST /api/system/users/{id}/status`、`POST /api/system/users/{id}/reset-password` |
| 参数 | 用户名、姓名、角色、状态；重置密码不回传密码哈希 |
| 返回 | 用户分页列表或保存结果；需要 `system:user:*` 权限 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET /api/system/roles`、`PUT /api/system/roles/{roleCode}/permissions`、`POST /api/system/roles/{id}/status` |
| 参数 | 角色信息、权限 ID/权限码数组、状态 |
| 返回 | 角色列表或更新结果；管理员角色停用请求返回 409 |

## 3. 采购、销售、库存接口

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET/POST /api/purchase-orders`、`POST /api/purchase-orders/{id}/submit`、`/approve`、`/reject` |
| 参数 | 订单头、商品明细；审核/驳回传审核意见 |
| 返回 | 订单详情；状态不允许转换时返回 409 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `POST /api/purchase-receipts`、`POST /api/purchase-receipts/{id}/confirm` |
| 参数 | 来源采购单、仓库、本次实收明细、入库日期、备注 |
| 返回 | 确认后的入库单和库存变化摘要；重复确认返回 409 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET/POST /api/sales-orders`、`POST /api/sales-orders/{id}/submit`、`/approve`、`/reject` |
| 参数 | 客户、仓库、商品明细、成交价、日期、备注 |
| 返回 | 销售订单详情及累计出库数量 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `POST /api/stock-out-orders`、`POST /api/stock-out-orders/{id}/confirm` |
| 参数 | 来源销售单、本次出库明细、出库日期、备注 |
| 返回 | 出库结果；库存不足或并发冲突返回 409，并给出商品和可用量 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `POST /api/sales-returns`、`POST /api/sales-returns/{id}/submit`、`/approve`、`/confirm` |
| 参数 | 来源出库单、退货原因、退货明细、退货日期 |
| 返回 | 退货单与回库结果；超可退数量返回 409 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET /api/inventory-balances`、`GET /api/stock-flows` |
| 参数 | 商品、仓库、业务类型、来源单号、日期范围、分页参数 |
| 返回 | 库存余额或流水分页列表；流水只读 |

## 4. 报表接口

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET /api/reports/dashboard`、`/purchase-analysis`、`/sales-analysis`、`/inventory-balance`、`/low-stock` |
| 参数 | `startDate`、`endDate`、仓库/商品/供应商/客户等可选筛选条件 |
| 返回 | 实时汇总指标、趋势或分页明细；统计口径仅限已确认业务数据 |

| 对应的功能 | 内容 |
| --- | --- |
| path | `GET /api/reports/export?type=...` |
| 参数 | 报表类型及与列表相同的筛选参数 |
| 返回 | UTF-8 BOM CSV 文件；权限不足返回 403 |

# 测试与验收用例

| 编号 | 场景 | 操作 | 预期结果 |
| --- | --- | --- | --- |
| T-PER-01 | 空库初始化 | 创建空 `nova_erp` 数据库后启动后端 | Flyway 成功建表并写入角色、权限、管理员种子数据 |
| T-PER-02 | 重启持久化 | 新增商品、供应商、采购单后重启后端 | 数据、状态、明细均保持不变 |
| T-PER-03 | 主数据统一 | 设置页新增供应商并打开采购新建页 | 新供应商出现在采购下拉；停用后不可新选 |
| T-PER-04 | 入库一致性 | 审核采购单并确认入库 | 入库单、订单累计实收、库存余额、采购入库流水同步一致 |
| T-PER-05 | 出库库存不足 | 建立超过可用库存的出库单并确认 | 返回库存不足，出库单未确认、库存余额和流水均不变化 |
| T-PER-06 | 重复确认 | 对已确认出库单再次调用确认接口 | 返回状态冲突，不重复扣减库存，不新增流水 |
| T-PER-07 | 退货上限 | 对已全部退回的出库明细再次退货 | 返回超可退数量错误，库存不再增加 |
| T-PER-08 | 权限拦截 | 用销售员令牌请求用户管理接口 | 返回 403，用户表无任何变化 |
| T-PER-09 | 操作日志 | 完成一次采购审核、入库和角色授权 | 日志中可查到操作人、时间、业务编号和动作 |
| T-PER-10 | 报表一致性 | 确认一笔销售出库后刷新看板和库存报表 | 销售金额、出库量、库存余额与出库单一致 |

# 开发顺序与交付物

1. 完成 MySQL、Flyway、JPA 基础配置与空库启动验证。
2. 完成用户/角色/权限和基础主数据表、接口改造。
3. 完成库存余额/流水服务及事务测试。
4. 迁移采购入库、销售出库、销售退货业务单据。
5. 改造报表查询与系统设置前端数据源。
6. 完成接口测试、核心事务测试、手工冒烟和数据库备份恢复说明。

交付物包括：Flyway SQL 脚本、Entity/Repository/Service/Controller 代码、数据库配置示例、初始化账号说明、接口文档、测试记录和数据备份说明。

