# NOVA ERP 经营概览 PRD

| 项目 | 内容 |
| --- | --- |
| 文档版本 | V1.0（评审稿） |
| 文档状态 | 待评审；确认后进入开发文档与实现阶段 |
| 所属模块 | 工作台 / 经营概览 |
| 关联版本 | ERP V2 持久化版本 |
| 目标用户 | 系统管理员、经营管理者；其他业务角色按权限看到精简待办 |

## 1. 背景与问题

当前系统左侧已预留“工作台 → 经营概览”入口，但该入口尚未能跳转到独立页面。业务报表中的“经营看板”已经能够展示采购金额、销售金额、库存货值、预警数量和近 7 日趋势，但它更偏向报表浏览，不能帮助用户快速判断“今天要处理什么”。

本功能将经营概览建设为登录/打开系统后的默认工作台：在一个页面中展示关键经营指标、待处理事项、库存风险和近期业务动态，并提供进入对应业务模块的快捷入口。

## 2. 产品目标

1. 管理者在 10 秒内了解当前经营规模、库存风险和待处理业务。
2. 采购、销售、仓库人员进入系统后可看到与本角色相关的待办，并一键跳转处理。
3. 所有统计均读取后端已持久化的业务数据；刷新页面或重启后端后，数据口径保持一致。
4. 明确区分“经营概览（行动入口）”与“业务报表（分析与导出）”：概览不替代报表明细。

## 3. 范围与优先级

### 3.1 Must

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| OVR-01 | 独立工作台入口 | 左侧“经营概览”可点击；默认进入该页面，导航、面包屑和选中状态正确。 |
| OVR-02 | 经营指标卡 | 展示本月销售金额、采购金额、销售出库量、当前库存货值、主仓库存预警数。 |
| OVR-03 | 业务待办 | 展示待审核采购单、待入库采购单、待审核销售单、待出库销售单的数量与快捷跳转。 |
| OVR-04 | 库存风险 | 展示缺货/低于安全库存商品，包含商品、主仓可用库存、安全库存、缺口和“去补货”入口。 |
| OVR-05 | 近期动态 | 展示最近 10 条采购入库、销售出库、销售退货、手工出入库流水。 |
| OVR-06 | 数据刷新与状态 | 首次进入自动加载；提供刷新按钮、加载态、空态和后端不可用提示。 |
| OVR-07 | 数据口径 | 概览仅统计已确认入库/出库/退货与当前库存余额；不将草稿单计入金额。 |

### 3.2 Should

| 编号 | 功能 | 说明 |
| --- | --- | --- |
| OVR-08 | 时间范围 | 支持“本月 / 近 7 日 / 今日”切换，指标和趋势同步变化。 |
| OVR-09 | 经营趋势 | 展示近 7 日采购金额、销售金额与出入库数量趋势；无数据时展示零值，不伪造数据。 |
| OVR-10 | 角色化待办 | 登录权限完成后，按角色隐藏无权处理的待办卡及快捷操作。 |

### 3.3 Won't

- 财务利润、毛利率、应收应付、现金流与预算达成率；当前没有可靠成本/财务数据源。
- 自定义大屏、拖拽布局、订阅推送、消息中心。
- 复杂 BI 图表、同比环比、多组织对比。

## 4. 页面设计与交互

### 4.1 页面结构

```text
经营概览
├─ 顶部：标题、数据更新时间、刷新按钮、时间范围（Should）
├─ 指标区：销售金额｜采购金额｜库存货值｜主仓库存预警
├─ 待办区：待采购审核｜待采购入库｜待销售审核｜待销售出库
├─ 左侧：近期经营趋势（Should）
├─ 右侧：库存风险清单
└─ 底部：近期业务动态
```

### 4.2 指标定义

| 指标 | 口径 | 默认范围 | 点击行为 |
| --- | --- | --- | --- |
| 销售金额 | 已确认销售出库明细的成交单价 × 已出库数量之和 | 本自然月 | 跳转业务报表的经营看板 |
| 采购金额 | 已确认采购入库明细对应采购单价 × 实收数量之和 | 本自然月 | 跳转采购分析 |
| 销售出库量 | 已确认销售出库单的出库数量合计 | 本自然月 | 跳转销售管理的出库记录 |
| 库存货值 | 当前启用/停用商品库存 × 商品当前售价的汇总估值 | 当前时点 | 跳转库存余额 |
| 主仓库存预警 | 主仓可用库存小于安全库存的商品数量 | 当前时点 | 跳转库存预警报表 |

说明：库存货值是按销售价计算的**参考估值**，不是财务存货成本；页面需明确标注。主仓库存预警用于提示主仓发货能力，商品总库存可能仍然充足（例如部分库存位于其他仓库）；风险行必须同时展示总库存、主仓可用库存与安全库存，避免混淆。

### 4.3 待办规则

| 待办 | 判定条件 | 跳转目标 |
| --- | --- | --- |
| 待采购审核 | 采购订单状态为“待审核” | 采购管理，预设状态筛选“待审核” |
| 待采购入库 | 采购订单状态为“已审核”且未入库数量大于 0 | 采购管理，定位可入库订单 |
| 待销售审核 | 销售订单状态为“待审核” | 销售管理，预设状态筛选“待审核” |
| 待销售出库 | 销售订单状态为“已审核”且待出库数量大于 0 | 销售管理，定位可出库订单 |

若数量为 0，仍显示卡片但显示“暂无待处理事项”，不制造红点或紧急状态。

### 4.4 库存风险与动态

库存风险默认最多显示 5 条，按“缺货优先、缺口数量倒序、商品编码正序”排列；超过时显示“查看全部预警”。

近期动态默认显示最近 10 条库存流水，字段为：时间、业务类型、商品、仓库、变动数量、来源单号。业务类型使用现有“采购入库、销售出库、销售退货、手工入库、手工出库”，点击来源单号后续可扩展为打开详情。

## 5. 数据与接口需求

### 5.1 概览接口

新增 `GET /api/dashboard/overview`，一次返回页面首屏需要的数据，避免前端并发拼接多份统计而产生口径不一致。

请求参数：

| 参数 | 必填 | 示例 | 说明 |
| --- | --- | --- | --- |
| range | 否 | `month` | `today`、`7d`、`month`；默认 `month` |
| riskLimit | 否 | `5` | 风险商品返回数量，默认 5，最大 20 |
| activityLimit | 否 | `10` | 动态返回数量，默认 10，最大 30 |

响应字段：

| 分组 | 字段 | 说明 |
| --- | --- | --- |
| meta | range、generatedAt | 实际统计范围与生成时间 |
| metrics | salesAmount、purchaseAmount、stockOutQuantity、inventoryValue、lowStockCount | 指标区数据 |
| todos | purchaseApproval、purchaseReceipt、salesApproval、salesStockOut | 每项返回数量、标题、跳转模块与建议筛选条件 |
| risks | productId、sku、name、warehouseName、totalStock、availableQuantity、safetyStock、gap、warningType | 库存风险列表；`totalStock` 为全仓总库存，`availableQuantity` 为主仓可用库存 |
| activities | flowNo、businessType、productName、warehouseName、changeQuantity、sourceNo、time | 最近库存流水 |
| trend（Should） | date、purchaseAmount、salesAmount、stockInQuantity、stockOutQuantity | 时间趋势数据 |

### 5.2 数据一致性要求

1. 所有金额和数量由后端计算，前端不得自行汇总业务单据。
2. 已确认采购入库、销售出库、销售退货完成后，刷新概览必须反映最新结果。
3. 同一个请求中的指标、待办、风险和动态基于同一时刻读取；读取失败时整页提示失败，不展示混合的新旧数据。
4. 金额使用两位小数；数量按商品单位展示；接口传输数字而非已格式化字符串。

## 6. 权限与异常

当前登录权限尚未完成时，所有演示用户可查看概览；完成 RBAC 后采用以下规则：

| 角色 | 可见范围 |
| --- | --- |
| 系统管理员、经营管理者 | 全部指标、待办、风险和动态 |
| 采购员/采购主管 | 采购指标、采购待办、库存风险、与采购有关动态 |
| 销售员/销售主管 | 销售指标、销售待办、库存风险、与销售有关动态 |
| 仓库管理员 | 库存货值、库存风险、入出库待办和全部库存动态 |

错误处理：接口 401/403 显示“无权查看经营概览”；接口 5xx 或网络失败显示“暂时无法读取经营数据”，保留刷新按钮；无任何业务数据时显示零值和明确空态。

## 7. 验收标准

| 编号 | 场景 | 通过标准 |
| --- | --- | --- |
| AC-01 | 导航 | 点击左侧经营概览后进入独立工作台，菜单高亮、面包屑正确。 |
| AC-02 | 指标口径 | 确认一笔采购入库和销售出库后，刷新概览，金额与业务报表/来源单据一致。 |
| AC-03 | 待办跳转 | 待审核采购单数量正确；点击后进入采购管理并自动筛选待审核单据。 |
| AC-04 | 库存风险 | 将商品库存降至安全库存以下后，风险清单出现该商品，缺口计算正确。 |
| AC-05 | 近期动态 | 手工入库、采购入库、销售出库、销售退货均按时间倒序出现在动态中。 |
| AC-06 | 持久化 | 重启前后端后，概览读取的数据与重启前已保存业务数据一致。 |
| AC-07 | 空态与异常 | 无单据时不报错；关闭后端时页面有可理解错误提示且可刷新。 |

## 8. 与现有业务报表的关系

现有“业务报表 → 经营看板”保留，继续承载报表浏览、趋势表和 CSV 导出。新“经营概览”不复制完整报表，而是复用相同的数据口径，强调待办与快捷处理。

开发时应抽取统一的后端统计服务，避免两个页面各自计算同一指标而出现销售金额或库存预警数量不一致。
