# 业务背景

经营概览是 NOVA ERP 的默认工作台，解决当前左侧“经营概览”入口不可用、经营数据与待处理业务分散在采购、销售、仓储和业务报表页面的问题。

该页面面向管理者和业务人员提供“先判断、再处理”的入口：指标用于快速了解经营状态，待办用于进入业务处理，库存风险用于提醒补货，近期动态用于追溯最近库存变化。它不承担完整报表导出或财务核算职责；复杂分析仍保留在“业务报表”模块。

现有 `ReportService` 已能计算经营看板的部分指标，仓储模块已能查询库存流水和余额。本次开发以一个统一的概览接口聚合这些数据，避免前端同时请求多个接口后自行计算而导致口径不一致。

# 功能梳理

| 编号 | 功能 | 前端实现 | 后端实现 | 优先级 |
| --- | --- | --- | --- | --- |
| OVR-01 | 工作台入口 | 左侧“经营概览”绑定 `switchModule('overview')`；默认模块改为 `overview` | 无新增写接口 | Must |
| OVR-02 | 经营指标 | 展示销售金额、采购金额、销售出库量、库存货值、主仓库存预警；金额统一格式化为人民币 | 按已确认单据和当前库存计算 `metrics` | Must |
| OVR-03 | 业务待办 | 渲染四张待办卡；点击携带状态筛选切换至采购或销售模块 | 统计待审核、可入库、待出库数量并返回跳转参数 | Must |
| OVR-04 | 库存风险 | 展示前 5 条风险商品；提供“查看全部预警”跳转 | 按缺货优先、缺口倒序、编码正序计算风险列表 | Must |
| OVR-05 | 近期动态 | 展示最近 10 条库存流水，正负数量使用不同视觉状态 | 从库存流水按时间倒序截取 | Must |
| OVR-06 | 刷新与状态 | 首次进入加载；提供刷新、加载、空态和错误提示 | `GET` 接口一次返回完整概览数据 | Must |
| OVR-07 | 口径复用 | 跳转到业务报表、采购/销售模块时不重复计算金额 | 抽取公共统计方法供概览和业务报表复用 | Must |
| OVR-08 | 时间范围 | 可选“今日、近 7 日、本月”，切换后重新拉取接口 | 校验 `range`，计算起止日期与趋势 | Should |
| OVR-09 | 经营趋势 | 使用表格或轻量趋势图显示每日采购/销售金额与出入库量 | 返回时间区间内按日聚合的 `trend` | Should |
| OVR-10 | 角色化可见 | 登录完成后根据权限隐藏无关待办和跳转按钮 | 概览接口依据当前用户权限过滤字段 | Should |

页面组件建议拆分如下：

```text
App.vue
└─ OverviewView.vue
   ├─ OverviewHeader（标题、更新时间、刷新、时间范围）
   ├─ OverviewMetrics（指标卡）
   ├─ OverviewTodos（待办卡）
   ├─ OverviewRiskList（库存风险）
   ├─ OverviewTrend（Should）
   └─ OverviewActivities（近期动态）
```

# 数据库设计

经营概览是只读聚合功能，不新增“经营概览结果表”，不将计算结果持久化，避免指标因缓存或重复存储而失真。每次请求均基于当前已保存业务数据计算。

当前项目的运行态业务数据统一保存在 MySQL 的 `erp_application_state` 快照中，后端恢复为商品、采购、销售、仓储等领域对象后进行聚合；后续完全切换为明细表读写时，概览接口保持不变，仅替换统计数据访问层。

## 表名：erp_application_state（现有）

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| id | TINYINT | PK，固定为 1 | 全局业务状态标识 | 概览不直接修改 |
| state_json | LONGTEXT | NOT NULL | 商品、单据、库存余额、库存流水等业务快照 | 概览读取恢复后的服务数据 |
| updated_at | DATETIME | NOT NULL | 最近一次成功业务写入时间 | 可作为数据更新时间辅助信息 |

## 统计数据来源（逻辑实体）

| 逻辑实体 | 当前来源 | 概览用途 | 过滤规则 |
| --- | --- | --- | --- |
| 商品 | `ProductService` / 商品快照 | 库存货值、风险行总库存 | 库存货值包含现存商品；风险行的 `totalStock` 为商品全仓总库存 |
| 采购订单、采购入库单 | `PurchaseService` / 采购快照 | 采购金额、待审核、待入库、趋势 | 金额仅统计状态“已确认”的入库单 |
| 销售订单、销售出库单 | `SalesService` / 销售快照 | 销售金额、出库量、待审核、待出库、趋势 | 金额/数量仅统计状态“已确认”的出库单 |
| 库存余额 | `WarehouseService` / 仓储快照 | 风险商品的仓库可用量与缺口 | 默认按主仓展示；后续可按仓库扩展 |
| 库存流水 | `WarehouseService` / 仓储快照 | 近期动态 | 按 `time DESC`，最多返回 `activityLimit` 条 |

## 后续明细表落地约束

当业务读写完全迁移到现有 `md_product`、`pur_purchase_order`、`wh_purchase_receipt`、`sal_sales_order`、`wh_stock_out_order`、`wh_inventory_balance`、`wh_stock_flow` 等表后，应建立以下查询索引，而非新建概览数据表：

| 表名 | 建议索引 | 用途 |
| --- | --- | --- |
| wh_stock_flow | `(time DESC)`、`(business_type, time)` | 最近动态、时间趋势 |
| wh_inventory_balance | `(warehouse_id, product_id)` | 库存风险与可用库存查询 |
| wh_purchase_receipt | `(status, stock_in_date)` | 采购金额、入库待办 |
| wh_stock_out_order | `(status, stock_out_date)` | 销售金额、出库待办 |
| pur_purchase_order | `(status)` | 待采购审核/待入库 |
| sal_sales_order | `(status)` | 待销售审核/待出库 |

# 业务逻辑

## OVR-01：进入经营概览

1. 应用初始 `activeModule` 设置为 `overview`。
2. 点击左侧“经营概览”后调用 `switchModule('overview')`。
3. 首次进入或用户点击刷新时执行 `loadOverview(range)`。
4. 请求成功后更新 `overviewData` 和 `overviewUpdatedAt`；请求失败只显示错误状态，不清空上一份成功数据。

## OVR-02：经营指标计算

1. 解析 `range`：`today` 为当天 00:00 至当前时刻，`7d` 为含当天的最近 7 个自然日，`month` 为当前自然月第一天至当前时刻；默认 `month`。
2. 采购金额：遍历状态为“已确认”且入库日期在范围内的采购入库单，以来源采购订单明细单价 × 实收数量求和，保留两位小数。
3. 销售金额：遍历状态为“已确认”且出库日期在范围内的销售出库单，以来源销售订单明细成交价 × 出库数量求和，保留两位小数。
4. 销售出库量：统计同一范围内已确认销售出库单的数量总和。
5. 库存货值：当前所有商品 `stock × price` 求和，返回 `inventoryValue`；前端标记为“按售价参考估值”。
6. 主仓库存预警数：使用与风险清单相同的主仓口径，统计 `主仓 availableQuantity < safetyStock` 的商品数量。库存等于安全库存时不预警；商品总库存仅用于辅助说明，不决定主仓预警。

## OVR-03：待办计算与跳转

1. 待采购审核：采购订单 `status = 待审核` 的数量。
2. 待采购入库：采购订单 `status = 已审核 AND pendingQuantity > 0` 的数量。
3. 待销售审核：销售订单 `status = 待审核` 的数量。
4. 待销售出库：销售订单 `status = 已审核 AND pendingQuantity > 0` 的数量。
5. 后端同时返回 `module`、`status`、`action`；前端根据这些字段执行切换与预设筛选，不能把单据状态文案硬编码在多个页面。
6. 待办为零时返回 `count: 0`，前端显示“暂无待处理事项”；不显示告警红点。

## OVR-04：库存风险

1. 从库存余额中计算商品在目标仓库的 `availableQuantity`；默认目标为主仓；同时从商品快照读取全仓 `totalStock`。
2. 商品无库存余额时按可用库存 0 处理。
3. 当 `availableQuantity <= 0` 时 `warningType = 缺货`；当 `availableQuantity < safetyStock` 时 `warningType = 库存偏低`。
4. `gap = max(safetyStock - availableQuantity, 0)`；风险行返回 `totalStock`、`availableQuantity`、`safetyStock`，让用户区分全仓存量与主仓发货能力。
5. 排序顺序：缺货优先 → 缺口数量倒序 → 商品编码正序；仅返回 `riskLimit` 条。
6. “查看全部预警”跳转业务报表的库存预警页；“去补货”跳转采购管理并可预填商品筛选条件。

## OVR-05：近期动态

1. 查询库存流水，按发生时间倒序排列。
2. 仅保留采购入库、销售出库、销售退货、手工入库、手工出库五种现有业务类型。
3. 返回前 `activityLimit` 条；默认 10，最大 30。
4. `changeQuantity > 0` 在前端显示为绿色正数，`changeQuantity < 0` 显示为蓝/红色负数；来源单号为空显示“手工登记”。

## OVR-06：数据一致性与异常

1. 概览接口在一个服务方法中完成所有聚合，先获得本次读取所需的商品、订单、入出库单、余额和流水快照，再计算结果。
2. 不修改商品、单据、库存余额和库存流水；接口仅为 `GET`。
3. 参数非法时返回 HTTP 400，例如 `range` 不是 `today`、`7d`、`month`，或数量限制超过最大值。
4. 无数据时返回空数组和数值 0，HTTP 仍为 200。
5. 数据库/服务读取异常时返回 HTTP 500；前端展示“暂时无法读取经营数据”及刷新按钮。

# 相关接口

| 对应的功能 | OVR-01 ~ OVR-09：加载经营概览 |
| ---------- | ---- |
| path | `GET /api/dashboard/overview` |
| 参数 | `range`：可选，`today` / `7d` / `month`，默认 `month`；`riskLimit`：可选，1~20，默认 5；`activityLimit`：可选，1~30，默认 10 |
| 返回 | HTTP 200，返回 `meta`、`metrics`、`todos`、`risks`、`activities`、`trend`；参数非法返回 HTTP 400；无权限（RBAC 完成后）返回 HTTP 403；服务异常返回 HTTP 500 |

返回示例：

```json
{
  "meta": { "range": "month", "startDate": "2026-09-01", "endDate": "2026-09-06", "generatedAt": "2026-09-06T16:50:00" },
  "metrics": { "salesAmount": 327.50, "purchaseAmount": 860.00, "stockOutQuantity": 7, "inventoryValue": 3078.40, "lowStockCount": 1 },
  "todos": [
    { "key": "purchaseApproval", "title": "待采购审核", "count": 0, "module": "purchases", "status": "待审核", "action": "review" },
    { "key": "salesStockOut", "title": "待销售出库", "count": 1, "module": "sales", "status": "已审核", "action": "stockOut" }
  ],
  "risks": [
    { "productId": 2, "sku": "SP-1002", "name": "黑色签字笔", "warehouseName": "主仓", "totalStock": 38, "availableQuantity": 18, "safetyStock": 30, "gap": 12, "warningType": "库存偏低" }
  ],
  "activities": [
    { "flowNo": "OUT20260906-00001", "businessType": "销售出库", "productName": "A4 复印纸", "warehouseName": "主仓", "changeQuantity": -5, "sourceNo": "OUT20260906-0001", "time": "2026-09-06T16:40:00" }
  ],
  "trend": [
    { "date": "2026-09-06", "purchaseAmount": 860.00, "salesAmount": 327.50, "stockInQuantity": 120, "stockOutQuantity": 7 }
  ]
}
```

| 对应的功能 | OVR-03 / OVR-04：快捷跳转 |
| ---------- | ---- |
| path | 不新增后端接口；前端使用 `switchModule(module)`，并传递 `status`、`action` 或商品筛选条件 |
| 参数 | `module`：`purchases` / `sales` / `reports`；可选 `status`、`action`、`productId` |
| 返回 | 切换至目标模块并应用预设筛选；目标模块首次打开时加载对应数据 |

## 开发自测清单

1. 冷启动后默认进入经营概览，左侧菜单与面包屑正确。
2. 创建、审核并确认采购入库后，采购金额、待采购入库数、库存风险和近期动态符合预期。
3. 创建、审核并确认销售出库后，销售金额、销售出库量、待销售出库数和近期动态符合预期。
4. 将库存降至安全库存以下，确认风险排序、缺口和跳转正确。
5. 后端关闭时页面显示错误；恢复后点击刷新可正常加载。
6. 完成一笔业务后重启前后端，概览数据仍与已保存数据一致。
