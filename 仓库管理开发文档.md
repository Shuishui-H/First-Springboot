# 业务背景

仓库管理是当前 ERP 进销存链路中的库存执行模块，承接商品档案、采购管理和销售管理产生的库存变化。系统需要回答三个问题：商品当前在哪个仓库、每个仓库还有多少可用库存、库存为什么发生变化。

当前项目已经实现仓库档案、库存余额、库存流水和手工出入库四部分功能，并与采购入库、销售出库、销售退货进行了库存联动。页面采用 Vue 3 单页模块切换，后端采用 Spring Boot 3.4.5 + Java 17 REST API。

当前版本为可运行演示版：数据保存在后端内存 `List` 中，服务重启后恢复初始数据；暂未接入 MySQL、登录认证、角色权限和真正的数据库事务。

# 功能梳理

## 1. 功能范围

### 1.1 仓库档案

- 查看仓库编码、名称、负责人和启用状态。
- 按仓库编码、名称或负责人搜索。
- 按启用状态筛选。
- 新增仓库档案。
- 编辑仓库档案。
- 启用或停用仓库。
- 删除仓库；仓库仍有库存时禁止删除。
- 修改仓库名称时同步刷新库存余额中的仓库名称。

### 1.2 库存余额

- 按“商品 + 仓库”维度查看库存。
- 查看现存数量、锁定数量、可用数量和安全库存。
- 按商品名称、商品编码或仓库名称搜索。
- 按仓库筛选。
- 根据安全库存显示库存正常或库存偏低。
- 通过“手工出入库”入口登记库存变化。

### 1.3 库存流水

- 查看每一笔库存变化。
- 查看流水号、业务类型、商品、仓库、变动数量、来源单号、操作人和时间。
- 按业务类型筛选。
- 按流水号、商品名称或商品编码搜索。
- 展示采购入库、销售出库、销售退货、手工入库和手工出库流水。

### 1.4 跨模块库存联动

- 采购入库确认：增加商品总库存、增加对应仓库余额、生成“采购入库”流水。
- 销售出库确认：扣减商品总库存、扣减对应仓库余额、生成“销售出库”流水。
- 销售退货确认：增加商品总库存、增加原销售出库仓库余额、生成“销售退货”流水。
- 仓储手工入库/出库：同时更新商品档案总库存、仓库余额和库存流水。
- 出库前校验分仓库存和商品总库存，任一库存不足都拒绝操作。

## 2. 页面结构

正式前端文件为 `erp-web/src/WarehouseView.vue`，由 `erp-web/src/App.vue` 通过侧边栏“仓储管理”入口加载。

页面包括：

1. 顶部统计：仓库数量、库存总量、库存预警、今日流水。
2. 仓库档案 Tab：仓库列表和仓库维护操作。
3. 库存余额 Tab：分仓库存查询和库存状态展示。
4. 库存流水 Tab：出入库流水查询和类型筛选。
5. 手工出入库弹窗：选择业务类型、仓库、商品、数量和备注。

## 3. 当前不包含的功能

- MySQL 持久化和数据库事务。
- 登录、用户、角色和权限控制。
- 库存锁定、销售订单库存预占。
- 仓库之间调拨。
- 库存盘点、盘盈盘亏。
- 批次、生产日期、保质期和序列号管理。
- Excel 导入导出。
- 分页、操作审计和库存预警通知。

# 数据库设计

当前代码使用内存对象模拟数据库表。下面给出与当前实体对应的逻辑数据库设计，后续接入 MySQL 时可按此结构落地。

## 1. 仓库表：`warehouse`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| id | BIGINT | PK | 仓库主键 | 当前使用内存自增 ID |
| code | VARCHAR(32) | NOT NULL, UNIQUE | 仓库编码 | 例如 `WH-MAIN` |
| name | VARCHAR(80) | NOT NULL | 仓库名称 | 例如“主仓” |
| manager | VARCHAR(40) | NULL | 仓库负责人 | 当前可为空 |
| status | VARCHAR(16) | NOT NULL | 仓库状态 | `启用` / `停用` |

当前 Java 实体：`com.erp.demo.warehouse.Warehouse`。

## 2. 库存余额表：`inventory_balance`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| id | BIGINT | PK | 库存余额主键 | 当前使用内存自增 ID |
| warehouse_id | BIGINT | NOT NULL, FK | 仓库 ID | 关联 `warehouse.id` |
| warehouse_name | VARCHAR(80) | NOT NULL | 仓库名称快照 | 当前用于直接展示 |
| product_id | BIGINT | NOT NULL | 商品 ID | 关联商品档案 |
| product_sku | VARCHAR(32) | NOT NULL | 商品编码快照 | 便于查询展示 |
| product_name | VARCHAR(80) | NOT NULL | 商品名称快照 | 便于查询展示 |
| quantity | INT | NOT NULL | 现存数量 | 不允许小于 0 |
| locked_quantity | INT | NOT NULL | 锁定数量 | 当前固定为 0 |
| available_quantity | INT | NOT NULL | 可用数量 | `quantity - locked_quantity` |
| safety_stock | INT | NOT NULL | 安全库存 | 从商品档案同步 |
| unit | VARCHAR(16) | NOT NULL | 计量单位 | 例如“包”“个” |

建议增加唯一约束：`UNIQUE(warehouse_id, product_id)`。

当前 Java 实体：`com.erp.demo.warehouse.InventoryBalance`。

## 3. 库存流水表：`stock_flow`

| 字段名 | 数据类型 | 约束 | 注释 | 备注 |
| ------ | -------- | ---- | ---- | ---- |
| id | BIGINT | PK | 流水主键 | 当前使用内存自增 ID |
| flow_no | VARCHAR(40) | NOT NULL, UNIQUE | 流水号 | 入库以 `IN` 开头，出库以 `OUT` 开头 |
| warehouse_id | BIGINT | NOT NULL | 仓库 ID | 发生库存变化的仓库 |
| warehouse_name | VARCHAR(80) | NOT NULL | 仓库名称快照 | 便于历史展示 |
| product_id | BIGINT | NOT NULL | 商品 ID | 关联商品档案 |
| product_sku | VARCHAR(32) | NOT NULL | 商品编码快照 | 便于查询展示 |
| product_name | VARCHAR(80) | NOT NULL | 商品名称快照 | 便于查询展示 |
| business_type | VARCHAR(20) | NOT NULL | 业务类型 | 采购入库、销售出库、销售退货、手工入库、手工出库 |
| change_quantity | INT | NOT NULL | 变动数量 | 入库为正数，出库为负数 |
| source_no | VARCHAR(64) | NULL | 来源单号或备注 | 业务单据使用关联单号 |
| operator | VARCHAR(40) | NOT NULL | 操作人 | 当前固定为“系统管理员” |
| time | DATETIME | NOT NULL | 发生时间 | 当前使用 `LocalDateTime` |

建议增加索引：`(warehouse_id, product_id)`、`(business_type)`、`(time)`。

## 4. 关联商品档案

商品档案当前由 `ProductService` 内存列表维护，核心字段包括 `id、sku、name、unit、stock、safetyStock、status`。其中：

- `product.stock` 表示商品总库存。
- `inventory_balance.quantity` 表示某一个仓库中的库存。
- 初始状态下，商品总库存全部同步到“主仓”。
- 后续每次库存变化同时更新商品总库存和对应分仓余额。

# 业务逻辑

## 1. 模块初始化

应用启动时，`WarehouseService` 初始化两个演示仓库：

- `WH-MAIN`：主仓，负责人张伟，启用。
- `WH-EAST`：华东仓，负责人李娜，启用。

随后读取 `ProductService.findAll()`，把每个商品的当前总库存创建为主仓库存余额，锁定数量为 0，可用数量等于现存数量。

因此初始化后满足：

```text
商品总库存 = 主仓库存余额 + 其他仓库库存余额
```

当前数据是内存数据，重启后会重新执行初始化。

## 2. 仓库档案维护

### 2.1 新增仓库

1. 校验仓库编码和名称不能为空。
2. 校验编码长度不超过 32 个字符，名称长度不超过 80 个字符。
3. 校验仓库编码不能重复，比较时忽略大小写。
4. 状态为空时默认设置为“启用”。
5. 使用 `AtomicLong` 生成仓库 ID。

### 2.2 修改仓库

1. 根据 ID 查询仓库，不存在返回 404。
2. 重新校验编码唯一性和字段长度。
3. 替换仓库档案对象。
4. 如果仓库名称发生变化，同步更新该仓库下所有库存余额的 `warehouseName`。

### 2.3 启用/停用

前端通过修改仓库档案的 `status` 实现启用或停用。停用仓库不能进行手工出入库，也不能被采购、销售业务作为有效库存仓库使用。

### 2.4 删除仓库

1. 查询仓库是否存在。
2. 检查该仓库是否有 `quantity > 0` 的库存余额。
3. 有库存时返回 409，拒绝删除。
4. 没有库存时删除仓库及其库存余额记录。

## 3. 库存余额查询

库存余额按照商品和仓库组合保存。查询时支持：

- `warehouseId`：只查看指定仓库。
- `productId`：只查看指定商品。
- `keyword`：匹配商品名称、商品编码或仓库名称。

库存状态规则：

```text
quantity < safetyStock  → 库存偏低
quantity >= safetyStock → 库存正常
```

当前 `lockedQuantity` 固定为 0，所以：

```text
availableQuantity = quantity
```

## 4. 手工出入库

入口为仓储页面右上角“手工出入库”。业务类型只允许：

- `手工入库`
- `手工出库`

处理步骤：

1. 校验仓库、商品和变动数量必填。
2. 校验数量必须大于 0。
3. 校验仓库存在且状态为“启用”。
4. 校验商品存在。
5. 查询“仓库 + 商品”的现有余额。
6. 手工出库时校验分仓库存充足，不能使余额小于 0。
7. 调用 `ProductService.increaseStock` 或 `decreaseStock` 更新商品总库存。
8. 新增或更新 `InventoryBalance`。
9. 生成 `StockFlow`，入库数量记录为正数，出库数量记录为负数。

如果仓库中尚无该商品余额：

- 手工入库可以创建新的库存余额。
- 手工出库直接拒绝，并提示该仓库没有此商品库存。

## 5. 采购/销售业务联动

采购和销售模块已经通过 `WarehouseService.recordBusinessMovement` 写入分仓库存和流水。该内部方法不再次修改商品总库存，避免重复计算；商品总库存由采购或销售服务先完成更新。

### 5.1 采购入库

采购入库确认时：

1. 采购单必须是“已审核”。
2. 入库数量不能超过采购单待入库数量。
3. 增加商品档案总库存。
4. 增加入库仓库的库存余额。
5. 生成“采购入库”流水，`sourceNo` 为采购入库单号。
6. 更新采购订单的已入库数量和状态。

### 5.2 销售出库

销售出库确认时：

1. 销售单必须是“已审核”。
2. 出库数量不能超过销售订单待出库数量。
3. 校验商品总库存和对应仓库分仓库存充足。
4. 扣减商品档案总库存。
5. 扣减销售订单仓库的库存余额。
6. 生成“销售出库”流水，`sourceNo` 为销售出库单号。
7. 更新销售订单已出库数量。

### 5.3 销售退货

销售退货必须引用已确认的销售出库单：

1. 出库单必须是“已确认”。
2. 退货数量不能超过该出库明细的可退数量。
3. 可退数量计算为：

```text
可退数量 = 已出库数量 - 已退货数量
```

4. 增加商品档案总库存。
5. 增加原销售出库仓库的库存余额。
6. 生成“销售退货”流水，`sourceNo` 为退货单号。
7. 更新销售订单和销售出库明细的已退货数量。

## 6. 并发与一致性

当前所有库存读写方法使用 `synchronized`，在单个应用进程内避免多个请求同时修改同一份内存库存。

当前版本仍存在以下限制：

- 商品库存、分仓余额和流水不是数据库事务。
- 服务异常退出时，内存数据不会恢复到上一次状态。
- 多实例部署时，`synchronized` 不能保证实例之间的一致性。

接入 MySQL 后，应使用 `@Transactional`，并通过数据库行锁或乐观锁保证库存并发安全。

## 7. 错误处理

| 场景 | HTTP 状态 | 当前提示 |
| --- | --- | --- |
| 仓库或商品不存在 | 404 | 仓库不存在 / 商品不存在 |
| 必填字段缺失 | 400 | 仓库与商品不能为空等 |
| 数量小于等于 0 | 400 | 变动数量必须大于 0 |
| 仓库已停用 | 400 | 仓库已停用，不能进行出入库操作 |
| 仓库库存不足 | 409 | 出库数量超过当前库存 |
| 商品总库存不足 | 409 | 商品库存不足 |
| 重复仓库编码 | 409 | 仓库编码已存在 |
| 有库存时删除仓库 | 409 | 该仓库仍有库存，不能删除 |

# 相关接口

接口统一前缀为 `/api`，前端通过 Vite 代理转发到 Spring Boot 的 8080 端口。

## 1. 查询仓库列表

| 对应的功能 | 仓库档案列表 |
| ---------- | ------------ |
| path | `GET /api/warehouses` |
| 参数 | 可选查询参数：`keyword`、`status` |
| 返回 | `Warehouse[]`，HTTP 200 |

示例：

```http
GET /api/warehouses?keyword=主仓&status=启用
```

## 2. 新建仓库

| 对应的功能 | 新增仓库档案 |
| ---------- | ------------ |
| path | `POST /api/warehouses` |
| 参数 | JSON：`code`、`name`、`manager`、`status` |
| 返回 | 新建后的 `Warehouse`，HTTP 201 |

请求示例：

```json
{
  "code": "WH-SOUTH",
  "name": "华南仓",
  "manager": "王强",
  "status": "启用"
}
```

## 3. 修改仓库

| 对应的功能 | 编辑仓库档案 |
| ---------- | ------------ |
| path | `PUT /api/warehouses/{id}` |
| 参数 | 路径参数 `id`；JSON：`code`、`name`、`manager`、`status` |
| 返回 | 更新后的 `Warehouse`，HTTP 200 |

## 4. 删除仓库

| 对应的功能 | 删除仓库档案 |
| ---------- | ------------ |
| path | `DELETE /api/warehouses/{id}` |
| 参数 | 路径参数 `id` |
| 返回 | 无响应体，HTTP 204；有库存时返回 HTTP 409 |

## 5. 查询库存余额

| 对应的功能 | 库存余额查询 |
| ---------- | ------------ |
| path | `GET /api/inventory` |
| 参数 | 可选查询参数：`warehouseId`、`productId`、`keyword` |
| 返回 | `InventoryBalance[]`，HTTP 200 |

## 6. 查询库存流水

| 对应的功能 | 库存流水查询 |
| ---------- | ------------ |
| path | `GET /api/stock-flows` |
| 参数 | 可选查询参数：`warehouseId`、`productId`、`businessType`、`keyword` |
| 返回 | `StockFlow[]`，HTTP 200 |

示例：

```http
GET /api/stock-flows?businessType=销售出库&keyword=A4
```

## 7. 手工出入库登记

| 对应的功能 | 手工入库 / 手工出库 |
| ---------- | ------------------- |
| path | `POST /api/stock-flows` |
| 参数 | JSON：`warehouseId`、`productId`、`businessType`、`quantity`、`remark` |
| 返回 | 新生成的 `StockFlow`，HTTP 201 |

请求示例：

```json
{
  "warehouseId": 1,
  "productId": 1,
  "businessType": "手工入库",
  "quantity": 10,
  "remark": "盘点调整"
}
```

## 8. 模块内部联动方法

该方法供采购、销售服务调用，不作为前端公开接口：

| 对应的功能 | 业务单据写入仓储余额和流水 |
| ---------- | -------------------------- |
| 方法 | `WarehouseService.recordBusinessMovement(...)` |
| 参数 | `warehouseId`、`productId`、`quantity`、入库方向、业务类型、来源单号、备注 |
| 返回 | 新生成的 `StockFlow` |

支持的业务类型为：`采购入库`、`销售出库`、`销售退货`、`手工入库`、`手工出库`。

# 前端交互与测试

## 1. 使用流程

1. 启动后端服务。
2. 启动前端 Vite 开发服务。
3. 访问 `http://localhost:5173`。
4. 点击左侧“仓储管理”。
5. 在“仓库档案”查看或维护仓库。
6. 在“库存余额”查看商品分仓库存。
7. 点击“手工出入库”登记库存变化。
8. 切换到“库存流水”查看刚刚产生的流水。
9. 回到商品档案，确认商品总库存已经同步变化。
10. 完成采购入库、销售出库或销售退货后，在库存流水中按业务类型查看来源单号。

## 2. 已完成的自测场景

| 场景 | 预期结果 | 实测结果 |
| --- | --- | --- |
| 查询仓库 | 返回主仓、华东仓 | 通过 |
| 新增/修改/删除空仓库 | CRUD 成功 | 通过 |
| 手工入库 3 件 | 商品总库存和分仓余额增加 3 | 通过 |
| 手工出库 2 件 | 商品总库存和分仓余额减少 2 | 通过 |
| 采购入库 | 生成采购入库流水并增加库存 | 通过 |
| 销售出库 | 生成销售出库流水并扣减库存 | 通过 |
| 销售退货 | 生成销售退货流水并回增库存 | 通过 |
| 库存不足出库 | 返回 409，不允许负库存 | 已覆盖逻辑 |
| JDK17 后端构建 | Maven BUILD SUCCESS | 通过 |
| Vue 前端构建 | Vite build 成功 | 通过 |

## 3. 后续 MySQL 改造建议

1. 在 `pom.xml` 引入 Spring Data JPA 和 MySQL 驱动。
2. 将三个 record 实体改造为 JPA Entity，补充 `@Table` 和字段映射。
3. 增加 `WarehouseRepository`、`InventoryBalanceRepository` 和 `StockFlowRepository`。
4. 用数据库查询替换内存 `List`。
5. 在库存变更服务上增加 `@Transactional`。
6. 对库存余额行增加版本号或数据库行锁。
7. 把库存流水来源单号与采购入库单、销售出库单、销售退货单建立可追溯关联。
8. 将当前固定操作人替换为登录用户。

